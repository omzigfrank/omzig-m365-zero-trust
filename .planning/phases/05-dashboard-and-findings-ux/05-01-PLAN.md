---
phase: 05-dashboard-and-findings-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/audit/src/remediation/types.ts
  - packages/audit/src/remediation/entra-id-remediation.ts
  - packages/audit/src/remediation/nist-zta-remediation.ts
  - packages/audit/src/remediation/nist-80053-remediation.ts
  - packages/audit/src/remediation/nist-csf-remediation.ts
  - packages/audit/src/remediation/index.ts
  - packages/audit/src/index.ts
  - packages/audit/src/remediation/__tests__/remediation-registry.test.ts
  - packages/db/src/control-plane/schema.ts
  - apps/web/src/components/ui/Drawer.tsx
  - apps/web/src/components/ui/MultiSelectDropdown.tsx
  - apps/web/src/components/audit/PowerShellBlock.tsx
autonomous: true
requirements:
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "Every control ID in the registry has a corresponding remediation entry"
    - "Remediation entries for actionable controls include step-by-step instructions"
    - "Remediation entries for applicable controls include admin portal URLs"
    - "Remediation entries for applicable controls include PowerShell commands"
    - "Advisory controls (warn) have organizational guidance text"
    - "Drawer component renders as portal, handles Escape and backdrop click"
    - "MultiSelectDropdown shows checkbox list with count badge"
    - "PowerShellBlock highlights keywords and supports copy-to-clipboard"
  artifacts:
    - path: "packages/audit/src/remediation/index.ts"
      provides: "getRemediationByControlId() lookup function"
      exports: ["getRemediationByControlId"]
    - path: "packages/audit/src/remediation/types.ts"
      provides: "RemediationEntry interface"
      exports: ["RemediationEntry"]
    - path: "packages/audit/src/remediation/entra-id-remediation.ts"
      provides: "29 CISA SCuBA Entra ID remediation entries"
      exports: ["ENTRA_ID_REMEDIATION"]
    - path: "packages/audit/src/remediation/nist-zta-remediation.ts"
      provides: "31 NIST 800-207 ZTA remediation entries"
      exports: ["NIST_ZTA_REMEDIATION"]
    - path: "packages/audit/src/remediation/nist-80053-remediation.ts"
      provides: "22 NIST 800-53 remediation entries"
      exports: ["NIST_80053_REMEDIATION"]
    - path: "packages/audit/src/remediation/nist-csf-remediation.ts"
      provides: "19 NIST CSF 2.0 remediation entries"
      exports: ["NIST_CSF_REMEDIATION"]
    - path: "apps/web/src/components/ui/Drawer.tsx"
      provides: "Slide-over panel component"
      exports: ["Drawer"]
    - path: "apps/web/src/components/ui/MultiSelectDropdown.tsx"
      provides: "Multi-select dropdown with checkboxes"
      exports: ["MultiSelectDropdown"]
    - path: "apps/web/src/components/audit/PowerShellBlock.tsx"
      provides: "Syntax-highlighted PowerShell code block with copy"
      exports: ["PowerShellBlock"]
    - path: "packages/db/src/control-plane/schema.ts"
      provides: "actionQueueDismissals table and criticalFindingsCount column"
      contains: "actionQueueDismissals"
  key_links:
    - from: "packages/audit/src/remediation/index.ts"
      to: "packages/audit/src/index.ts"
      via: "re-export in barrel"
      pattern: "export.*getRemediationByControlId"
    - from: "packages/audit/src/remediation/index.ts"
      to: "packages/audit/src/registry/control-registry.ts"
      via: "control IDs must match between registries"
      pattern: "getAllControls.*controlId"
---

<objective>
Build the foundation components for Phase 5: the remediation data registry (all 101 controls), reusable UI primitives (Drawer, MultiSelectDropdown, PowerShellBlock), and DB schema additions (actionQueueDismissals table, criticalFindingsCount column on tenants).

Purpose: These are the building blocks that Plans 02 (findings UX) and 03 (action queue) both depend on. The remediation registry is the single largest data artifact. The UI primitives are reusable across the phase. The DB schema changes must be pushed before API routes reference them.

Output: Remediation registry with 101 entries, 3 UI components, updated control-plane schema
</objective>

<execution_context>
@C:/Users/FrankDiaz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FrankDiaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-and-findings-ux/05-CONTEXT.md
@.planning/phases/05-dashboard-and-findings-ux/05-RESEARCH.md
@.planning/phases/05-dashboard-and-findings-ux/05-VALIDATION.md

@packages/audit/src/types.ts
@packages/audit/src/index.ts
@packages/audit/src/registry/control-registry.ts
@packages/audit/src/registry/entra-id-controls.ts
@packages/audit/src/registry/nist-zta-controls.ts
@packages/audit/src/registry/nist-80053-controls.ts
@packages/audit/src/registry/nist-csf-controls.ts
@packages/db/src/control-plane/schema.ts
@apps/web/src/components/ui/Badge.tsx
@apps/web/src/components/ui/Card.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/audit/src/types.ts:
```typescript
export interface ControlDefinition {
  id: string;
  product: string;
  description: string;
  requirementLevel: RequirementLevel;
  severity: Severity;
  nist80053: string;
  nistCsf?: string;
  nist800207Tenet?: string;
  evaluator: EvaluatorFn;
  requiredPermissions: string[];
}
```

From packages/audit/src/registry/control-registry.ts:
```typescript
export function getAllControls(): ControlDefinition[];
export function getControlById(id: string): ControlDefinition | undefined;
export function getControlsByProduct(product: string): ControlDefinition[];
```

From packages/db/src/control-plane/schema.ts:
```typescript
export const tenants = mssqlTable('tenants', {
  // ... existing columns ...
  lastAuditScore: int('last_audit_score'),
  // criticalFindingsCount column needs to be added
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remediation registry with types, data for all 101 controls, and tests</name>
  <files>
    packages/audit/src/remediation/types.ts,
    packages/audit/src/remediation/entra-id-remediation.ts,
    packages/audit/src/remediation/nist-zta-remediation.ts,
    packages/audit/src/remediation/nist-80053-remediation.ts,
    packages/audit/src/remediation/nist-csf-remediation.ts,
    packages/audit/src/remediation/index.ts,
    packages/audit/src/index.ts,
    packages/audit/src/remediation/__tests__/remediation-registry.test.ts
  </files>
  <behavior>
    - Test: every control ID from getAllControls() has a matching remediation entry in getRemediationByControlId()
    - Test: each RemediationEntry has a non-empty steps array (at least 1 step)
    - Test: entries for product 'AAD' have adminPortalUrl set (all 29 SCuBA controls have portal links)
    - Test: at least 8 entries have non-empty powershell field (8 SCuBA controls have explicit PS commands per CISA baseline)
    - Test: getRemediationByControlId returns undefined for unknown control ID
    - Test: no duplicate control IDs across all remediation arrays
  </behavior>
  <action>
    1. Create `packages/audit/src/remediation/types.ts`:
       ```typescript
       export interface RemediationEntry {
         controlId: string;
         steps: string[];
         adminPortalUrl?: string;
         powershell?: string;
         estimatedImpact?: string;
         notes?: string;
       }
       ```

    2. Create `packages/audit/src/remediation/entra-id-remediation.ts` with 29 entries matching the CISA SCuBA Entra ID controls (IDs: MS.AAD.1.1v1 through MS.AAD.7.9v1). For each entry:
       - `steps`: Reference CISA SCuBA baseline document (cisagov/ScubaGear baselines/aad.md) for canonical remediation instructions. Steps should be admin-friendly numbered instructions.
       - `adminPortalUrl`: Use the verified Entra admin portal deep links from RESEARCH.md (e.g., `https://entra.microsoft.com/#view/Microsoft_AAD_ConditionalAccess/ConditionalAccessBlade/~/Policies` for CA policy controls).
       - `powershell`: Include Graph API PowerShell commands for the 8+ controls that have them. Use `Connect-MgGraph` and `New-Mg*`/`Set-Mg*`/`Update-Mg*` cmdlets.
       - Advisory controls (4.1, 7.3, 7.4, 7.6, 7.8, 7.9): steps should say "This is an organizational/advisory control. Verify with your security team." No powershell needed.

    3. Create `packages/audit/src/remediation/nist-zta-remediation.ts` with 31 entries matching NIST_ZTA_CONTROLS (IDs: NIST.ZTA.T1.1v1 through NIST.ZTA.T7.4v1). These map to the same Entra ID settings as SCuBA controls but framed in terms of NIST 800-207 tenet requirements. Include adminPortalUrl where the setting has a portal page. Include powershell where applicable. Advisory entries (T6.3 emergency access) get organizational guidance.

    4. Create `packages/audit/src/remediation/nist-80053-remediation.ts` with 22 entries matching NIST_80053_CONTROLS (IDs: NIST.80053.AC-2v1 through NIST.80053.SI-4v1). Frame guidance in terms of 800-53 control families. Include adminPortalUrl and powershell where applicable.

    5. Create `packages/audit/src/remediation/nist-csf-remediation.ts` with 19 entries matching NIST_CSF_CONTROLS (IDs: NIST.CSF.PR.AC-1v1 through NIST.CSF.RC.RP-1v1). Frame guidance in terms of CSF functions. Advisory entries (GV.OC-1, RC.RP-1) get organizational guidance.

    6. Create `packages/audit/src/remediation/index.ts`:
       - Import all 4 remediation arrays
       - Build a Map<string, RemediationEntry> from all arrays
       - Export `getRemediationByControlId(id: string): RemediationEntry | undefined`
       - Export the RemediationEntry type from types.ts

    7. Update `packages/audit/src/index.ts` to re-export:
       ```typescript
       export { getRemediationByControlId } from './remediation/index.js';
       export type { RemediationEntry } from './remediation/types.js';
       ```

    8. Create test file `packages/audit/src/remediation/__tests__/remediation-registry.test.ts` with the behavior tests listed above. Import `getAllControls` from the control registry and `getRemediationByControlId` from the remediation index to verify coverage.

    IMPORTANT: The control IDs in remediation entries MUST exactly match the IDs in the control registry files. Read each control definition file to extract the exact IDs before writing remediation entries.
  </action>
  <verify>
    <automated>cd C:/Users/FrankDiaz/omzig-m365-zero-trust && pnpm --filter @omzig/audit vitest run src/remediation/__tests__/remediation-registry.test.ts -x</automated>
  </verify>
  <done>
    All 101 controls have remediation entries with non-empty steps arrays. All 29 AAD controls have adminPortalUrl. At least 8 controls have PowerShell commands. getRemediationByControlId returns correct entries by ID and undefined for unknown IDs. Tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: UI primitive components and DB schema additions</name>
  <files>
    apps/web/src/components/ui/Drawer.tsx,
    apps/web/src/components/ui/MultiSelectDropdown.tsx,
    apps/web/src/components/audit/PowerShellBlock.tsx,
    packages/db/src/control-plane/schema.ts
  </files>
  <action>
    1. Create `apps/web/src/components/ui/Drawer.tsx` -- a portal-based slide-over panel:
       - Props: `isOpen: boolean`, `onClose: () => void`, `title: string`, `children: React.ReactNode`, `width?: string` (default "400px")
       - Use `createPortal(document.body)` to escape parent stacking contexts
       - Semi-transparent backdrop (bg-black/30) with click-to-close
       - Right-sliding panel with `role="dialog"`, `aria-modal="true"`, `aria-label={title}`
       - Auto-focus close button on open, Escape key handler to close
       - Body scroll lock via `document.body.style.overflow = "hidden"` on open
       - CSS transition: `transition-transform duration-300 ease-in-out`
       - Close button with lucide X icon
       - Scrollable content area (`overflow-y-auto`)
       - Reference the code example in RESEARCH.md but use the project's "use client" directive pattern

    2. Create `apps/web/src/components/ui/MultiSelectDropdown.tsx` -- a reusable multi-select dropdown:
       - Props: `label: string`, `options: { value: string; label: string }[]`, `selected: Set<string>`, `onChange: (selected: Set<string>) => void`, `minSelected?: number` (default 0)
       - Button shows label + count of active selections when count < total: e.g., "Severity (2)"
       - Dropdown body: absolute-positioned below button, checkbox list
       - Click-outside-to-close via useEffect with document mousedown listener
       - `aria-expanded`, `aria-haspopup="listbox"`, `role="listbox"`, `role="option"` with `aria-selected`
       - z-50 for dropdown to stay above other content
       - Respect minSelected: if removing would go below min, prevent deselection
       - Use lucide ChevronDown icon
       - Reference the code example in RESEARCH.md

    3. Create `apps/web/src/components/audit/PowerShellBlock.tsx` -- syntax-highlighted code block:
       - Props: `code: string`
       - CSS-class-based syntax highlighting (NO Prism or highlight.js per user decision):
         - Graph API cmdlets (Connect-MgGraph, Get-Mg*, Set-Mg*, New-Mg*, Update-Mg*, Invoke-MgGraphRequest): blue-600 font-semibold
         - Parameters (-Scopes, -BodyParameter, etc.): cyan-600
         - Strings (single and double quoted): amber-600
         - Comments (# to end of line): gray-500 italic
       - HTML-escape first (& < >), then apply regex replacements in correct order (comments first to avoid highlighting inside comments, then strings, then keywords, then params)
       - Dark code block background (bg-gray-900, text-gray-100)
       - Copy-to-clipboard button using `navigator.clipboard.writeText()`:
         - Top-right corner of code block
         - Shows lucide Copy icon, switches to Check icon (green-400) for 2 seconds after copy
         - `aria-label="Copy to clipboard"`
       - `dangerouslySetInnerHTML` is safe here because input is static registry data, not user input
       - "use client" directive

    4. Update `packages/db/src/control-plane/schema.ts`:
       - Add `criticalFindingsCount` column to tenants table: `criticalFindingsCount: int('critical_findings_count').notNull().default(0)`
       - Add new `actionQueueDismissals` table:
         ```
         id: varchar('id', { length: 36 }).primaryKey().notNull()
         orgId: varchar('org_id', { length: 36 }).notNull().references(() => organizations.id)
         userId: varchar('user_id', { length: 36 }).notNull().references(() => users.id)
         itemKey: varchar('item_key', { length: 200 }).notNull()
         dismissedAt: datetime2('dismissed_at').notNull().default(sql`GETDATE()`)
         ```
       - itemKey format: "critical-{tenantId}" or "status-{tenantId}-needs_reauth" or "status-{tenantId}-stale_audit"

    After creating all files, run `drizzle-kit push` if the project uses push for schema changes (check package.json scripts), or generate migration as appropriate. Then run the full web test suite to verify no regressions.
  </action>
  <verify>
    <automated>cd C:/Users/FrankDiaz/omzig-m365-zero-trust && pnpm --filter @omzig/web exec tsc --noEmit && pnpm -r test</automated>
  </verify>
  <done>
    Drawer component renders via portal with Escape/backdrop close and ARIA attributes. MultiSelectDropdown renders checkbox list with count badges and click-outside close. PowerShellBlock highlights PS keywords/params/strings/comments with copy button. DB schema has criticalFindingsCount on tenants and actionQueueDismissals table. All existing tests still pass. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @omzig/audit vitest run src/remediation/__tests__/remediation-registry.test.ts -x` passes -- all 101 controls covered
2. `pnpm --filter @omzig/web exec tsc --noEmit` passes -- no type errors in new components
3. `pnpm -r test` passes -- no regressions
4. `getRemediationByControlId('MS.AAD.1.1v1')` returns entry with steps, adminPortalUrl, and powershell
5. `getRemediationByControlId('NONEXISTENT')` returns undefined
</verification>

<success_criteria>
- Remediation registry has exactly 101 entries matching all control IDs from getAllControls()
- All 29 AAD entries have adminPortalUrl
- At least 8 entries have PowerShell commands
- Drawer, MultiSelectDropdown, and PowerShellBlock components exist and compile
- control-plane schema has actionQueueDismissals table and criticalFindingsCount column
- All tests pass (new and existing)
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-and-findings-ux/05-01-SUMMARY.md`
</output>
