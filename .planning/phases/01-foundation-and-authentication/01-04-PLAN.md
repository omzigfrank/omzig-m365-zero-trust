---
phase: 01-foundation-and-authentication
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - apps/web/src/lib/msal.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/hooks/useAuth.ts
  - apps/web/src/hooks/useApi.ts
  - apps/web/src/components/layout/AuthGuard.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/layout.tsx
  - apps/web/Dockerfile
  - apps/web/next.config.mjs
  - bicep/platform/container-apps.bicep
  - bicep/platform/sql-elastic-pool.bicep
  - bicep/platform/keyvault.bicep
  - bicep/platform/signalr.bicep
  - bicep/platform/vnet.bicep
  - bicep/platform/acr.bicep
  - bicep/platform/main-platform.bicep
  - apps/api/src/index.ts
  - apps/api/src/routes/tenants.ts
autonomous: false
requirements:
  - INFRA-01
  - INFRA-05
  - AUTH-05
  - AUTH-07

must_haves:
  truths:
    - "A developer can open the Next.js frontend in a browser, click 'Sign in with Microsoft', authenticate via Entra ID, and see their role displayed"
    - "The frontend sends authenticated API requests to the Hono backend with Bearer tokens and receives responses"
    - "Role-restricted UI elements are shown or hidden based on the authenticated user's role"
    - "The Bicep template validates and deploys all platform infrastructure: Container Apps, SQL Elastic Pool, Key Vault, SignalR, VNet"
    - "Azure SQL is accessible only via private endpoint from the Container Apps VNet (AUTH-07)"
    - "SignalR Service is provisioned in serverless mode (INFRA-05)"
    - "The API wires all middleware (auth, MFA, RBAC, tenant context) and routes (health, auth, tenants) into a working endpoint -- tenants route returns 501 as a stub for Phase 4"
  artifacts:
    - path: "apps/web/src/lib/msal.ts"
      provides: "MSAL configuration with tenant-specific authority and API scopes"
      contains: "PublicClientApplication"
    - path: "apps/web/src/lib/api-client.ts"
      provides: "Authenticated HTTP client for calling the Hono API"
      exports: ["apiClient", "apiGet", "apiPost"]
    - path: "apps/web/src/hooks/useAuth.ts"
      provides: "React hook for authentication state including roles"
      exports: ["useAuth"]
    - path: "apps/web/Dockerfile"
      provides: "Multi-stage Docker build for Next.js standalone output"
      contains: "standalone"
    - path: "bicep/platform/main-platform.bicep"
      provides: "Composition module for all platform infrastructure"
      contains: "module"
    - path: "bicep/platform/sql-elastic-pool.bicep"
      provides: "SQL Server + Elastic Pool + control plane DB with private endpoint"
      contains: "Microsoft.Sql/servers"
    - path: "bicep/platform/keyvault.bicep"
      provides: "Key Vault with managed identity access policies"
      contains: "Microsoft.KeyVault/vaults"
    - path: "bicep/platform/signalr.bicep"
      provides: "SignalR Service in serverless mode"
      contains: "Serverless"
    - path: "bicep/platform/vnet.bicep"
      provides: "VNet with subnets for Container Apps and private endpoints"
      contains: "Microsoft.Network/virtualNetworks"
    - path: "apps/api/src/routes/tenants.ts"
      provides: "Stub tenants route returning 501 Not Implemented (full implementation in Phase 4)"
      contains: "501"
  key_links:
    - from: "apps/web/src/lib/api-client.ts"
      to: "apps/api/src/index.ts"
      via: "MSAL token in Authorization header to Hono API"
      pattern: "Authorization.*Bearer"
    - from: "apps/web/src/hooks/useAuth.ts"
      to: "apps/web/src/lib/msal.ts"
      via: "MSAL instance for token acquisition"
      pattern: "acquireTokenSilent|msalInstance"
    - from: "bicep/platform/container-apps.bicep"
      to: "bicep/platform/vnet.bicep"
      via: "Container Apps Environment uses VNet subnet"
      pattern: "subnetId"
    - from: "bicep/platform/sql-elastic-pool.bicep"
      to: "bicep/platform/vnet.bicep"
      via: "Private endpoint for SQL Server in VNet"
      pattern: "privateEndpoint"
    - from: "bicep/platform/keyvault.bicep"
      to: "bicep/platform/container-apps.bicep"
      via: "Managed identity access policy for Container Apps"
      pattern: "accessPolicies|principalId"
    - from: "apps/api/src/index.ts"
      to: "apps/api/src/routes/tenants.ts"
      via: "Tenants route registration at /api/tenants"
      pattern: "tenantsRoutes|/api/tenants"
---

<objective>
Wire the frontend to the API with authenticated requests, create all Bicep platform infrastructure modules, and integrate all components into a working end-to-end flow. This is the integration plan that connects Plan 02 (API + auth) and Plan 03 (database + secrets) to the frontend and Azure infrastructure.

Purpose: INFRA-01 (Next.js on Container Apps), INFRA-05 (SignalR), AUTH-05 (managed identity in Bicep), and AUTH-07 (private endpoints) complete the platform infrastructure. This plan proves the full auth flow works end-to-end and creates the deployment templates.
Output: Updated Next.js app with auth flow, Bicep modules for all platform services, Dockerfiles for both apps, wired API with all routes.
</objective>

<execution_context>
@C:/Users/FrankDiaz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FrankDiaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-and-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-and-authentication/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-authentication/01-02-SUMMARY.md
@.planning/phases/01-foundation-and-authentication/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 02: API middleware and routes -->
From apps/api/src/middleware/auth.ts:
```typescript
export function createAuthMiddleware(tenantId: string, clientId: string): MiddlewareHandler;
```

From apps/api/src/middleware/mfa.ts:
```typescript
export function requireMfa(): MiddlewareHandler;
```

From apps/api/src/middleware/rbac.ts:
```typescript
export function requireRole(...allowedRoles: Role[]): MiddlewareHandler;
export function requirePermission(permission: Permission): MiddlewareHandler;
```

From apps/api/src/routes/auth.ts:
```typescript
// GET /api/auth/me -> ApiResponse<UserProfile>
export const authRoutes: Hono;
```

From apps/api/src/routes/health.ts:
```typescript
// GET /api/health -> HealthResponse
export const healthRoutes: Hono;
```

<!-- From Plan 03: Database and Key Vault -->
From apps/api/src/middleware/tenant.ts:
```typescript
export function withTenantDb(): MiddlewareHandler;
```

From apps/api/src/services/tenant-db.ts:
```typescript
export function provisionTenantDatabase(orgId: string, displayName: string, m365TenantId: string, token: string): Promise<TenantRef>;
export function deprovisionTenantDatabase(tenantId: string): Promise<void>;
```

From apps/api/src/services/keyvault.ts:
```typescript
export function getSecret(name: string): Promise<string>;
export function setSecret(name: string, value: string): Promise<void>;
export function getTenantToken(tenantId: string): Promise<string>;
```

From packages/db/src/connection.ts:
```typescript
export function getControlPlaneDb(): Promise<DrizzleDb>;
export function getTenantDb(databaseName: string): Promise<{ db: DrizzleDb; pool: ConnectionPool }>;
```

<!-- From @omzig/shared -->
From packages/shared/src/types/roles.ts:
```typescript
export type Role = 'Owner' | 'Admin' | 'Analyst' | 'Read-only';
export interface UserProfile { id: string; email: string; displayName: string; baseRole: Role; orgId: string; orgName: string; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend auth update + API client + Dockerfiles</name>
  <files>
    apps/web/src/lib/msal.ts,
    apps/web/src/lib/api-client.ts,
    apps/web/src/hooks/useAuth.ts,
    apps/web/src/hooks/useApi.ts,
    apps/web/src/components/layout/AuthGuard.tsx,
    apps/web/src/app/page.tsx,
    apps/web/src/app/layout.tsx,
    apps/web/next.config.mjs,
    apps/web/Dockerfile
  </files>
  <action>
    1. Update `apps/web/src/lib/msal.ts`:
       - Change authority from `common` to tenant-specific: `https://login.microsoftonline.com/${NEXT_PUBLIC_AZURE_TENANT_ID}`
       - Add apiScopes: `[api://${NEXT_PUBLIC_MSAL_CLIENT_ID}/access_as_user]` for backend API calls
       - Add OmzigTokenClaims interface extending IdTokenClaims with roles?: string[] and amr?: string[]
       - Keep cacheLocation: "sessionStorage" (per research)
       - Export loginRequest, apiScopes, msalInstance, OmzigTokenClaims

    2. Update `apps/web/src/hooks/useAuth.ts`:
       - Use useMsal() from @azure/msal-react
       - Expose: isAuthenticated, user (name, email), roles (from idTokenClaims.roles), isLoading, login(), logout()
       - Parse roles from account.idTokenClaims using OmzigTokenClaims type cast
       - Determine highest role from roles array using ROLE_HIERARCHY from @omzig/shared
       - Include hasMfa boolean from amr claim

    3. Create `apps/web/src/lib/api-client.ts`:
       - Create an authenticated fetch wrapper that:
         a. Calls msalInstance.acquireTokenSilent() to get a fresh access token
         b. Falls back to acquireTokenRedirect() if silent fails
         c. Adds Authorization: Bearer {token} header
         d. Adds Content-Type: application/json
         e. Adds X-Tenant-Id header when tenantId is provided
         f. Parses response as ApiResponse<T>
         g. Handles 401 (redirect to login), 403 (show error), 5xx (show error with retry option)
       - Export: apiGet<T>(url, tenantId?), apiPost<T>(url, body, tenantId?), apiPut<T>, apiDelete<T>

    4. Create `apps/web/src/hooks/useApi.ts`:
       - React hook wrapping api-client for use in components
       - Provides: data, error, isLoading, refetch
       - Handles loading states and error states

    5. Update `apps/web/src/components/layout/AuthGuard.tsx`:
       - Extend existing auth guard to check roles
       - Accept requiredRole prop (optional) -- if specified, check user has sufficient role
       - Accept requiredPermission prop (optional) -- check via hasPermission from @omzig/shared
       - Show "Insufficient permissions" message if role check fails
       - Keep existing redirect-to-login behavior for unauthenticated users

    6. Update `apps/web/src/app/layout.tsx`:
       - Ensure MsalProvider wraps the app
       - Corporate/enterprise visual tone: clean background, muted colors, blue primary brand (per user decision)
       - Light theme only (dark mode deferred per user decision)

    7. Update `apps/web/src/app/page.tsx`:
       - For authenticated users: call GET /api/auth/me and display UserProfile (name, email, role, org name)
       - Show a simple dashboard landing with "Welcome, {name}" and role badge
       - For unauthenticated users: show login button with Omzig branding
       - This is a minimal landing page -- full dashboard UX comes in Phase 5

    8. Ensure `apps/web/next.config.mjs` has output: "standalone" (should already be set from Plan 01).

    9. Create `apps/web/Dockerfile`:
       - Multi-stage build with node:22-alpine base
       - Builder: install deps, copy source, run `next build`
       - Runner: non-root user (nextjs:1001), copy .next/standalone and .next/static and public
       - EXPOSE 3000, ENV PORT=3000, CMD ["node", "server.js"]
       - Note: This standalone Dockerfile works for the web app in isolation. For monorepo builds, the CI pipeline will need to copy workspace packages before building. Document this in the Dockerfile comments.
  </action>
  <verify>
    <automated>cd /c/Users/FrankDiaz/omzig-m365-zero-trust && pnpm --filter web build</automated>
  </verify>
  <done>
    - MSAL uses tenant-specific authority (not common)
    - API client sends authenticated requests with Bearer tokens
    - useAuth hook exposes roles from Entra ID app roles claim
    - AuthGuard enforces role-based route protection
    - Landing page shows authenticated user's profile and role
    - Next.js builds with output: "standalone"
    - Dockerfile produces a containerized Next.js app
  </done>
</task>

<task type="auto">
  <name>Task 2: Bicep platform infrastructure modules and stub tenants route</name>
  <files>
    bicep/platform/vnet.bicep,
    bicep/platform/acr.bicep,
    bicep/platform/container-apps.bicep,
    bicep/platform/sql-elastic-pool.bicep,
    bicep/platform/keyvault.bicep,
    bicep/platform/signalr.bicep,
    bicep/platform/main-platform.bicep,
    apps/api/src/routes/tenants.ts,
    apps/api/src/index.ts
  </files>
  <action>
    1. Create `bicep/platform/vnet.bicep`:
       - Parameters: location, orgName, environment (dev/staging/prod)
       - VNet: address space 10.0.0.0/16
       - Subnets:
         - container-apps-subnet (10.0.0.0/21) -- Container Apps Environment needs /21 minimum
         - private-endpoints-subnet (10.0.1.0/24) -- for SQL and Key Vault private endpoints
       - Output: vnetId, containerAppsSubnetId, privateEndpointsSubnetId

    2. Create `bicep/platform/acr.bicep`:
       - Parameters: location, orgName, environment
       - Azure Container Registry (Basic SKU for dev, Standard for prod)
       - Admin user enabled for Container Apps pull (or use managed identity pull)
       - Output: acrLoginServer, acrName

    3. Create `bicep/platform/container-apps.bicep`:
       - Parameters: location, orgName, environment, containerAppsSubnetId, acrLoginServer, apiImageTag, webImageTag
       - Container Apps Environment in the VNet subnet
       - Container App: omzig-api (Hono backend)
         - Image from ACR
         - System-assigned managed identity (for Key Vault, SQL access -- AUTH-05)
         - Environment variables: AZURE_TENANT_ID, AZURE_CLIENT_ID, FRONTEND_URL, KEY_VAULT_URL, SQL_SERVER_HOST, CONTROL_PLANE_DB_NAME
         - No secrets in env vars -- use Key Vault references where possible
         - Ingress: external, port 8080, HTTPS only
         - Scale: min 1, max 10, HTTP scaling rule
         - Liveness probe: /api/health, period 30s
         - Readiness probe: /api/health, period 10s
       - Container App: omzig-web (Next.js frontend)
         - Image from ACR
         - Ingress: external, port 3000, HTTPS only
         - Scale: min 1, max 5
         - Environment variables: NEXT_PUBLIC_API_URL (pointing to api container app FQDN)
       - Output: apiPrincipalId (managed identity), apiFqdn, webFqdn

    4. Create `bicep/platform/sql-elastic-pool.bicep`:
       - Parameters: location, orgName, environment, privateEndpointsSubnetId, vnetId, apiPrincipalId
       - SQL Server: name `sql-omzig-{environment}`
         - Azure AD admin: the deployment principal
         - Minimum TLS 1.2
         - Public network access: Disabled (AUTH-07 -- private endpoint only)
       - Elastic Pool: Standard tier, 50 eDTUs (dev), configurable for prod
         - Per-database DTU min: 0, max: 50
       - Control plane database: `omzig-controlplane` (within elastic pool)
       - Private Endpoint for SQL Server in the private-endpoints subnet
       - Private DNS Zone: `privatelink.database.windows.net` linked to VNet
       - RBAC: Grant apiPrincipalId the "SQL DB Contributor" role on the SQL Server (for dynamic DB creation)
       - TDE enabled by default (Azure manages encryption keys)
       - Output: sqlServerFqdn, controlPlaneDbName, elasticPoolName

    5. Create `bicep/platform/keyvault.bicep`:
       - Parameters: location, orgName, environment, apiPrincipalId
       - Key Vault: name `kv-omzig-{environment}`
         - SKU: standard
         - Soft delete: enabled (90 days)
         - Purge protection: enabled
         - RBAC authorization: enabled (use Azure RBAC instead of access policies)
       - Role assignments:
         - apiPrincipalId gets "Key Vault Secrets User" role (read secrets)
         - apiPrincipalId gets "Key Vault Crypto User" role (encrypt/decrypt)
       - Output: keyVaultUrl, keyVaultName

    6. Create `bicep/platform/signalr.bicep`:
       - Parameters: location, orgName, environment
       - SignalR Service:
         - Name: `sigr-omzig-{environment}`
         - SKU: Free_F1 (dev) or Standard_S1 (prod)
         - Service mode: Serverless (INFRA-05 requirement)
         - CORS: allow frontend URL
       - Output: signalRConnectionString, signalREndpoint

    7. Create `bicep/platform/main-platform.bicep`:
       - Composition module that deploys all sub-modules in correct order:
         a. VNet (no dependencies)
         b. ACR (no dependencies)
         c. Container Apps (depends on VNet, ACR)
         d. SQL Elastic Pool (depends on VNet, Container Apps for principalId)
         e. Key Vault (depends on Container Apps for principalId)
         f. SignalR (no dependencies)
       - Parameters: location, orgName, environment, apiImageTag, webImageTag
       - Pass outputs between modules (VNet IDs to Container Apps and SQL, principalId to Key Vault and SQL)
       - Output: all FQDNs, connection info

    8. Create `apps/api/src/routes/tenants.ts`:
       - Create a Hono router for /api/tenants
       - Stub all endpoints to return 501 Not Implemented with a structured ApiResponse:
         `{ error: { code: 'NOT_IMPLEMENTED', message: 'Tenant management endpoints will be available in Phase 4' } }`
       - Stub routes: GET / (list tenants), POST / (provision), GET /:id (get tenant), DELETE /:id (deprovision)
       - Export as tenantsRoutes
       - This is a placeholder proving the route exists and the middleware chain works. Full implementation comes in Phase 4 (Tenant Onboarding).

    9. Update `apps/api/src/index.ts`:
       - Import and register tenantsRoutes at /api/tenants (after auth middleware, requires authentication)
       - Wire all middleware: JWK -> MFA -> RBAC and tenant context middleware on tenant-specific subroutes
       - Ensure the middleware chain order is: health (no auth) -> JWK -> MFA -> auth routes -> RBAC -> tenant routes

    10. Validate Bicep: Run `az bicep build --file bicep/platform/main-platform.bicep` to verify syntax.

    IMPORTANT: These Bicep modules go in bicep/platform/ alongside (not replacing) the existing bicep/identity/, bicep/devices/, bicep/security/, bicep/data/ modules. The existing main.bicep is not modified.
  </action>
  <verify>
    <automated>cd /c/Users/FrankDiaz/omzig-m365-zero-trust && az bicep build --file bicep/platform/main-platform.bicep --stdout > /dev/null 2>&1 && echo "Bicep validation passed" || echo "Bicep validation failed"</automated>
  </verify>
  <done>
    - All 7 Bicep modules exist in bicep/platform/
    - main-platform.bicep composes all modules with correct dependency chain
    - SQL Server has public network access disabled + private endpoint (AUTH-07)
    - Key Vault uses RBAC authorization with managed identity access
    - SignalR is configured in Serverless mode (INFRA-05)
    - Container Apps have managed identities with roles for Key Vault and SQL (AUTH-05)
    - Elastic Pool configured with per-database DTU limits
    - `az bicep build` validates all templates without errors
    - Existing bicep/ modules are unchanged
    - apps/api/src/routes/tenants.ts exists and returns 501 for all endpoints
    - apps/api/src/index.ts registers tenants routes at /api/tenants
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end auth flow and infrastructure</name>
  <files>N/A</files>
  <action>
    Human verifies the full platform infrastructure and authentication flow built across all 4 plans:

    1. Monorepo (Plan 01): Turborepo + pnpm workspace with @omzig/shared, @omzig/db, @omzig/tsconfig packages
    2. API (Plan 02): Hono backend with JWK auth, MFA enforcement, 4-role RBAC with per-tenant overrides
    3. Database (Plan 03): Per-tenant DB isolation via Elastic Pool, Key Vault for secrets, tenant context middleware
    4. Frontend (Plan 04 Task 1): Next.js with MSAL auth, API client, role-based UI
    5. Infrastructure (Plan 04 Task 2): Bicep modules for Container Apps, SQL, Key Vault, SignalR, VNet with private endpoints
    6. Stub routes (Plan 04 Task 2): Tenants route returning 501 (full implementation in Phase 4)

    Verification steps:
    1. Run `pnpm build` from root -- verify all packages and apps compile
    2. Run `pnpm test` from root -- verify all unit tests pass
    3. Start the API locally: `cd apps/api && pnpm dev`
    4. Verify health endpoint: `curl http://localhost:8080/api/health` -- should return 200 with health status
    5. Verify tenants stub: `curl http://localhost:8080/api/tenants` -- should return 501 with NOT_IMPLEMENTED
    6. Start the web app locally: `cd apps/web && pnpm dev`
    7. Open http://localhost:3000 in browser
    8. Click "Sign in with Microsoft" -- verify Entra ID login redirects and returns
    9. After login, verify the landing page shows your name, email, and role
    10. Check browser Network tab: verify API calls to /api/auth/me include Authorization: Bearer header
    11. Verify Bicep: `az bicep build --file bicep/platform/main-platform.bicep` produces valid ARM template
  </action>
  <verify>
    <automated>cd /c/Users/FrankDiaz/omzig-m365-zero-trust && pnpm build && pnpm test</automated>
  </verify>
  <done>
    - Build and tests pass
    - Auth flow works end-to-end in browser
    - Tenants stub returns 501
    - Bicep templates validate
    - User approves the phase is complete
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` compiles all packages and apps
2. `pnpm test` runs all unit tests (health, auth, mfa, rbac, keyvault)
3. Frontend authenticates with Entra ID and calls API with Bearer token
4. API validates JWT, checks MFA, enforces RBAC
5. Tenants route returns 501 Not Implemented (stub for Phase 4)
6. Bicep templates validate with `az bicep build`
7. SQL Server uses private endpoint only (AUTH-07)
8. SignalR in serverless mode (INFRA-05)
9. Container Apps have managed identity with Key Vault and SQL access (AUTH-05)
</verification>

<success_criteria>
- Full auth flow: Browser -> MSAL login -> Entra ID -> JWT -> API validates -> returns profile with role
- Bicep deploys all platform infrastructure with correct security configuration
- Private endpoints for database access (no public internet)
- Managed identity for all service-to-service auth
- Tenants route registered and returning 501 stub
- All unit tests pass, builds succeed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-authentication/01-04-SUMMARY.md`
</output>
