---
phase: 08-drift-detection
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/src/components/drift/DriftAlertList.tsx
  - apps/web/src/components/drift/DriftAlertDetail.tsx
  - apps/web/src/components/drift/DriftSeverityBadge.tsx
  - apps/web/src/components/drift/DriftConfigPanel.tsx
  - apps/web/src/hooks/useDriftAlerts.ts
  - apps/web/src/hooks/useDriftConfig.ts
  - apps/web/src/app/drift/page.tsx
  - apps/web/src/components/layout/Sidebar.tsx
  - apps/web/src/app/tenants/page.tsx
  - apps/web/src/components/tenants/ActionQueue.tsx
  - apps/web/src/__tests__/DriftAlertList.test.tsx
  - apps/web/src/__tests__/DriftAlertDetail.test.tsx
  - apps/web/src/__tests__/useDriftAlerts.test.ts
autonomous: true
requirements:
  - DRIFT-03
  - DRIFT-04
  - DRIFT-05
  - DRIFT-06

must_haves:
  truths:
    - "User can navigate to /drift from the sidebar and see a table of drift alerts across all tenants they have access to"
    - "Drift alert list shows timestamp, severity badge, area name, activity type, actor UPN, and status (active/dismissed)"
    - "User can click a drift alert to see the full detail view with before/after JSON diff, human-readable summary, and actor information"
    - "Drift detail view links to Phase 7 remediation: a Remediate button appears for newly-failing controls"
    - "Drift alerts appear in the cross-tenant action queue on the /tenants dashboard page"
    - "New drift alerts push to the dashboard in real-time via SignalR without page refresh"
    - "User can filter drift alerts by severity and area"
    - "Admin users can configure the per-tenant drift check interval (15-60 minutes) via DriftConfigPanel"
    - "Sidebar shows a drift alert count badge when undismissed alerts exist"
  artifacts:
    - path: "apps/web/src/components/drift/DriftAlertList.tsx"
      provides: "Table component for drift alerts with timestamp, severity badge, area, activity, actor, status columns"
      exports: ["DriftAlertList"]
    - path: "apps/web/src/components/drift/DriftAlertDetail.tsx"
      provides: "Detail view with before/after JSON diff, summary, actor info, and Remediate link"
      exports: ["DriftAlertDetail"]
    - path: "apps/web/src/components/drift/DriftSeverityBadge.tsx"
      provides: "Colored badge for Critical (red), High (orange), Medium (yellow), Low (blue)"
      exports: ["DriftSeverityBadge"]
    - path: "apps/web/src/components/drift/DriftConfigPanel.tsx"
      provides: "Polling interval dropdown (15/30/45/60 min) with save for Admin users"
      exports: ["DriftConfigPanel"]
    - path: "apps/web/src/hooks/useDriftAlerts.ts"
      provides: "Fetch hook for drift alerts with filter state and SignalR real-time subscription"
      exports: ["useDriftAlerts"]
    - path: "apps/web/src/hooks/useDriftConfig.ts"
      provides: "Fetch and save hook for drift config (interval)"
      exports: ["useDriftConfig"]
    - path: "apps/web/src/app/drift/page.tsx"
      provides: "Drift alerts page at /drift route"
      exports: ["default"]
    - path: "apps/web/src/components/layout/Sidebar.tsx"
      provides: "Extended sidebar with Drift nav item and alert count badge"
      contains: "sync_problem"
    - path: "apps/web/src/components/tenants/ActionQueue.tsx"
      provides: "Extended action queue with drift_detected item type rendering"
      contains: "drift_detected"
  key_links:
    - from: "apps/web/src/hooks/useDriftAlerts.ts"
      to: "/api/tenants/:tenantId/drift-alerts"
      via: "fetch GET with severity/area filters"
      pattern: "fetch.*drift-alerts"
    - from: "apps/web/src/hooks/useDriftAlerts.ts"
      to: "SignalR hub"
      via: "connection.on('driftAlert', handler) subscription for real-time push"
      pattern: "driftAlert"
    - from: "apps/web/src/components/drift/DriftAlertDetail.tsx"
      to: "apps/web/src/components/remediation/RemediateButton.tsx"
      via: "Renders RemediateButton for newly-failing controls computed from before/after diff"
      pattern: "RemediateButton"
    - from: "apps/web/src/components/tenants/ActionQueue.tsx"
      to: "/drift"
      via: "drift_detected items link to the /drift page filtered by tenant"
      pattern: "drift_detected"
    - from: "apps/web/src/components/layout/Sidebar.tsx"
      to: "/drift"
      via: "Drift nav item links to /drift route"
      pattern: "sync_problem"
---

<objective>
Build the complete frontend drift detection UI: drift alert list with filtering, detail view with before/after JSON diff and remediation link, real-time SignalR push for new alerts, sidebar navigation with alert count badge, action queue integration, and drift polling config panel.

Purpose: MSPs need to see configuration drift events clearly and act on them quickly. This plan delivers the user-facing layer that consumes the backend drift engine from Plan 08-01, closing the feedback loop from detection to visibility to remediation.
Output: Drift alert list page at /drift, detail view with before/after comparison, severity badges, SignalR real-time subscription, sidebar badge, action queue drift items, config panel, and test coverage.
</objective>

<execution_context>
@C:/Users/FrankDiaz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FrankDiaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-drift-detection/08-CONTEXT.md
@.planning/phases/08-drift-detection/08-RESEARCH.md
@.planning/phases/08-drift-detection/08-01-SUMMARY.md

@apps/web/src/components/layout/Sidebar.tsx
@apps/web/src/components/tenants/ActionQueue.tsx
@apps/web/src/components/remediation/RemediateButton.tsx
@apps/web/src/hooks/useAudit.ts
@apps/web/src/hooks/useRemediation.ts
@apps/web/src/app/tenants/page.tsx
@apps/web/src/app/tenants/[id]/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From apps/web/src/components/layout/Sidebar.tsx (navItems array to extend):
```typescript
const navItems = [
  { href: "/dashboard", label: "Dashboard", icon: "dashboard" },
  { href: "/audit", label: "Audit", icon: "security" },
  { href: "/reports", label: "Reports", icon: "analytics" },
  { href: "/deploy", label: "Deploy", icon: "rocket_launch", disabled: true },
];
// Phase 8: Add { href: "/drift", label: "Drift", icon: "sync_problem" }
// Uses Material Symbols (already loaded in project via <link> in layout)
```

From apps/web/src/components/tenants/ActionQueue.tsx (item type rendering):
```typescript
// ActionQueueItem type is from @omzig/shared:
// type: 'critical_findings' | 'needs_reauth' | 'stale_audit' | 'drift_detected'
// Each type has a border color and icon -- Phase 8 adds 'drift_detected' rendering
const SEVERITY_BORDER: Record<string, string> = {
  critical_findings: 'border-l-red-500',
  needs_reauth: 'border-l-yellow-500',
  stale_audit: 'border-l-gray-400',
  // Phase 8: drift_detected: 'border-l-purple-500'
};
```

From apps/web/src/components/remediation/RemediateButton.tsx:
```typescript
interface RemediateButtonProps {
  finding: AuditFinding;
  tenantId: string;
  classification: 'SAFE' | 'RISKY';
  bundle: ScopeBundleName;
  impactSummary?: string;
  onJobCreated?: (jobId: string) => void;
}
export function RemediateButton({ finding, tenantId, classification, bundle, impactSummary, onJobCreated }: RemediateButtonProps);
// For drift -> remediation linking: create a synthetic AuditFinding from the drift alert's controlId
```

From apps/web/src/hooks/useAudit.ts (SignalR subscription pattern):
```typescript
// Existing pattern for real-time updates:
// 1. Negotiate via GET /api/auth/signalr/negotiate
// 2. Build HubConnection with accessToken
// 3. connection.on('auditProgress', handler)
// Phase 8 adds: connection.on('driftAlert', handler)
```

Expected API endpoints from Plan 08-01:
```typescript
// GET /api/tenants/:tenantId/drift-alerts?page=1&pageSize=20&severity=Critical&area=conditionalAccess&status=active
// Returns: { data: DriftAlert[], total: number, page: number, pageSize: number }
// DriftAlert (list): { id, area, severity, activityType, actorUpn, diffSummary, detectedAt, dismissedAt }

// GET /api/tenants/:tenantId/drift-alerts/:alertId
// Returns: { id, area, severity, activityType, actorUpn, beforeSnapshot, afterSnapshot, diffSummary, auditEventId, detectedAt, dismissedAt, dismissedBy, remediationJobId }

// POST /api/tenants/:tenantId/drift-alerts/:alertId/dismiss
// Returns: { ...updatedAlert }

// GET /api/tenants/:tenantId/drift-config
// Returns: { intervalMinutes: number, lastPollAt: string | null }

// PATCH /api/tenants/:tenantId/drift-config
// Body: { intervalMinutes: 15 | 30 | 45 | 60 }
// Returns: { intervalMinutes: number, lastPollAt: string | null }
```

From @omzig/audit (exported drift types from Plan 08-01):
```typescript
export type DriftSeverity = 'Critical' | 'High' | 'Medium' | 'Low';
export interface DriftAlertMessage {
  type: 'drift_detected';
  driftAlertId: string;
  tenantId: string;
  area: string;
  severity: DriftSeverity;
  activityType: string;
  actorUpn: string | null;
  diffSummary: string;
  detectedAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drift hooks, DriftAlertList, DriftSeverityBadge, DriftConfigPanel, /drift page, sidebar nav</name>
  <files>
    apps/web/src/hooks/useDriftAlerts.ts,
    apps/web/src/hooks/useDriftConfig.ts,
    apps/web/src/components/drift/DriftAlertList.tsx,
    apps/web/src/components/drift/DriftSeverityBadge.tsx,
    apps/web/src/components/drift/DriftConfigPanel.tsx,
    apps/web/src/app/drift/page.tsx,
    apps/web/src/components/layout/Sidebar.tsx,
    apps/web/src/__tests__/DriftAlertList.test.tsx
  </files>
  <behavior>
    - Test: DriftAlertList renders a table with columns: Time, Severity, Area, Activity, Actor, Status
    - Test: DriftAlertList renders DriftSeverityBadge for each alert with correct color class (Critical=red, High=orange, Medium=yellow, Low=blue)
    - Test: DriftAlertList shows "No drift alerts" empty state when data is empty
    - Test: DriftAlertList renders "Active" or "Dismissed" status badge per alert
    - Test: Clicking a row calls onAlertSelect callback with the alert id
    - Test: DriftAlertList severity filter dropdown filters displayed rows
    - Test: DriftAlertList area filter dropdown filters displayed rows
    - Test: DriftSeverityBadge renders correct text and background class for each severity level
    - Test: DriftConfigPanel renders interval dropdown with 15/30/45/60 options
    - Test: DriftConfigPanel Save button calls PATCH endpoint with selected interval
  </behavior>
  <action>
    1. Create `apps/web/src/hooks/useDriftAlerts.ts`:
       - Export `useDriftAlerts(tenantId: string | null)` hook.
       - State: `alerts: DriftAlert[]`, `total: number`, `loading: boolean`, `error: string | null`.
       - Filter state: `severity: string | null`, `area: string | null`, `status: 'active' | 'dismissed'` (default 'active'), `page: number` (default 1).
       - Fetch function: when tenantId is set, `GET /api/tenants/${tenantId}/drift-alerts?page=${page}&pageSize=20${severity ? '&severity='+severity : ''}${area ? '&area='+area : ''}&status=${status}` with Bearer token from useAuth().
       - Re-fetch when tenantId, page, severity, area, or status changes.
       - **SignalR subscription**: Reuse the existing negotiate + HubConnection pattern from useAudit.ts. After connecting, call `connection.on('driftAlert', (message: DriftAlertMessage) => { ... })`. When a drift_detected message arrives for the current tenantId, prepend the new alert to the list (optimistic real-time update) and increment total. If the message is for a different tenant, ignore (the Sidebar badge will handle cross-tenant counts separately).
       - Export filter setters: `setSeverityFilter`, `setAreaFilter`, `setStatusFilter`, `setPage`.
       - Export `dismissAlert(alertId: string)`: `POST /api/tenants/${tenantId}/drift-alerts/${alertId}/dismiss`. On success, update the alert in local state to show dismissed.
       - Return `{ alerts, total, loading, error, page, setSeverityFilter, setAreaFilter, setStatusFilter, setPage, dismissAlert }`.
       - **Note on cross-tenant mode**: When `tenantId` is null (the /drift page shows all tenants), the hook should fetch alerts across all accessible tenants. Implementation: fetch the tenant list first (reuse useTenants pattern), then fetch drift-alerts for each tenant in parallel. Merge and sort by detectedAt desc. This matches how the action queue works (server computes across tenants). Alternatively, if a cross-tenant drift endpoint exists from 08-01, use that. If not, implement the client-side merge.

    2. Create `apps/web/src/hooks/useDriftConfig.ts`:
       - Export `useDriftConfig(tenantId: string)` hook.
       - On mount, `GET /api/tenants/${tenantId}/drift-config` to load current interval and lastPollAt.
       - Export `saveConfig(intervalMinutes: number)`: `PATCH /api/tenants/${tenantId}/drift-config` with `{ intervalMinutes }`.
       - Return `{ intervalMinutes, lastPollAt, loading, error, saveConfig, saving }`.

    3. Create `apps/web/src/components/drift/DriftSeverityBadge.tsx`:
       - Props: `{ severity: 'Critical' | 'High' | 'Medium' | 'Low' }`.
       - Render a small inline badge (rounded-full, px-2.5, py-0.5, text-xs, font-medium).
       - Colors:
         - Critical: `bg-red-100 text-red-800`
         - High: `bg-orange-100 text-orange-800`
         - Medium: `bg-yellow-100 text-yellow-800`
         - Low: `bg-blue-100 text-blue-800`

    4. Create `apps/web/src/components/drift/DriftAlertList.tsx`:
       - Props: `{ alerts: DriftAlert[], onAlertSelect: (alertId: string) => void, onDismiss: (alertId: string) => void }`.
       - Render a table with columns:
         - **Time**: `detectedAt` formatted as relative time ("3 min ago", "2 hours ago") with full datetime in title attribute.
         - **Severity**: `<DriftSeverityBadge severity={alert.severity} />`.
         - **Area**: Human-readable area name (e.g., "Conditional Access" for "conditionalAccess", "Admin Roles" for "adminRoles"). Use a `AREA_DISPLAY_NAMES` lookup.
         - **Activity**: `activityType` truncated to 50 chars with tooltip for full text.
         - **Actor**: `actorUpn` or "System" if null.
         - **Status**: Green "Active" badge if `dismissedAt` is null, gray "Dismissed" badge if set.
         - **Actions**: "Dismiss" button (small, outline) for active alerts. Hidden for dismissed.
       - Clicking a row (except the Dismiss button) calls `onAlertSelect(alert.id)`.
       - Empty state: when alerts is empty, show a dashed border box with icon and message "No drift alerts detected" with subtext "Changes may take up to 15 minutes to appear after a configuration change" (per research audit event latency note).
       - Filter bar above the table:
         - Severity dropdown: All / Critical / High / Medium / Low.
         - Area dropdown: All / Conditional Access / Admin Roles / Security Defaults / Auth Methods / etc. (derived from ACTIVITY_TO_AREA areas).
         - These call the filter setters from useDriftAlerts.

    5. Create `apps/web/src/components/drift/DriftConfigPanel.tsx`:
       - Props: `{ tenantId: string }`.
       - Uses `useDriftConfig(tenantId)` hook.
       - Render a section with header "Drift Monitoring" with Material Symbols `sync` icon.
       - Dropdown for interval: 15 minutes, 30 minutes, 45 minutes, 60 minutes.
       - Save button that calls `saveConfig(selectedInterval)`.
       - Show success/error feedback inline (same pattern as ScheduleSettings from Phase 6).
       - Display "Last checked: {relative time}" below the dropdown if `lastPollAt` is set.
       - Disable the save button for non-Admin users (check role from useAuth).

    6. Create `apps/web/src/app/drift/page.tsx`:
       - "use client" directive.
       - Page title: "Configuration Drift" with Material Symbols `sync_problem` icon.
       - Tenant selector at top: dropdown of all accessible tenants (reuse useTenants). Default to "All Tenants" showing merged alerts. When a specific tenant is selected, filter to that tenant's alerts only.
       - Render `<DriftAlertList>` with data from `useDriftAlerts(selectedTenantId)`.
       - When an alert is clicked (`onAlertSelect`), show the detail view. Implementation: either navigate to a sub-route or open a drawer (Claude's discretion per CONTEXT -- a drawer off the list page is simpler and avoids a new route). Use the Drawer component pattern from Phase 5 (createPortal, click-outside close).
       - Below the alert list, if a specific tenant is selected, show `<DriftConfigPanel tenantId={...} />`.

    7. Update `apps/web/src/components/layout/Sidebar.tsx`:
       - Add a new nav item to the `navItems` array:
         ```typescript
         { href: "/drift", label: "Drift", icon: "sync_problem" },
         ```
         Insert it after the "Reports" item and before the "Deploy" item.
       - Add an optional badge to the Drift nav item showing the count of undismissed drift alerts across all tenants. To avoid a separate API call on every render, fetch the count lazily: add a `useDriftAlertCount()` hook (or inline the fetch) that calls `GET /api/action-queue` and counts items where `type === 'drift_detected'`. Display as a small red circle with white text (absolute positioned on the nav item) when count > 0.
       - Badge styling: `absolute -top-1 -right-1 flex h-5 w-5 items-center justify-center rounded-full bg-red-500 text-[10px] font-bold text-white`.

    8. Create test file `apps/web/src/__tests__/DriftAlertList.test.tsx`:
       - Render DriftAlertList with mock alerts (3 items: one Critical, one High, one Low).
       - Assert table renders 3 rows.
       - Assert DriftSeverityBadge appears for each row with correct severity text.
       - Assert empty state renders when alerts is empty.
       - Assert clicking a row calls onAlertSelect with correct id.
       - Assert "Dismissed" badge shown for dismissed alerts, "Active" for active.
       - Assert Dismiss button visible for active alerts only.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/__tests__/DriftAlertList.test.tsx -x</automated>
  </verify>
  <done>
    DriftAlertList renders filterable table with severity badges, area names, actor UPN, and dismiss button. DriftSeverityBadge renders colored badges for 4 severity levels. DriftConfigPanel lets admins set 15/30/45/60 min interval. /drift page shows cross-tenant drift alerts with tenant selector and drawer detail. Sidebar has "Drift" nav item with sync_problem icon and alert count badge. useDriftAlerts hook fetches alerts with filters and subscribes to SignalR driftAlert events for real-time updates. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DriftAlertDetail with before/after diff, remediation link, action queue integration, dashboard badge</name>
  <files>
    apps/web/src/components/drift/DriftAlertDetail.tsx,
    apps/web/src/components/tenants/ActionQueue.tsx,
    apps/web/src/app/tenants/page.tsx,
    apps/web/src/__tests__/DriftAlertDetail.test.tsx,
    apps/web/src/__tests__/useDriftAlerts.test.ts
  </files>
  <behavior>
    - Test: DriftAlertDetail renders area name, severity badge, activity type, and actor UPN
    - Test: DriftAlertDetail renders human-readable diff summary
    - Test: DriftAlertDetail renders before/after JSON sections with syntax highlighting
    - Test: DriftAlertDetail shows "Changes may take up to 15 minutes to appear" latency notice
    - Test: DriftAlertDetail renders a Remediate button when newly-failing controls are detected from the diff
    - Test: DriftAlertDetail dismiss button calls dismiss callback
    - Test: ActionQueue renders drift_detected items with purple left border and Activity icon
    - Test: ActionQueue drift items link to /drift page
    - Test: useDriftAlerts prepends new alert when SignalR driftAlert message arrives for current tenantId
    - Test: useDriftAlerts ignores SignalR messages for different tenantId
    - Test: useDriftAlerts dismissAlert sends POST and updates local state
  </behavior>
  <action>
    1. Create `apps/web/src/components/drift/DriftAlertDetail.tsx`:
       - Props: `{ alertId: string, tenantId: string, onClose: () => void, onDismiss: (alertId: string) => void }`.
       - On mount, fetch `GET /api/tenants/${tenantId}/drift-alerts/${alertId}` with Bearer token.
       - Render a detail panel (or drawer content) with sections:

       **Header section:**
       - Area name (human-readable from AREA_DISPLAY_NAMES lookup) + DriftSeverityBadge.
       - Activity type text.
       - Detected at: formatted full datetime.
       - Actor: UPN or "System action" if null.
       - Status: Active/Dismissed badge.

       **Summary section:**
       - "What Changed" heading.
       - Render `diffSummary` text in a styled box (bg-gray-50, rounded, p-4, text-sm).
       - Below the summary, display the latency notice: "Note: Configuration changes may take up to 15 minutes to appear in the audit log." in a subtle info box (bg-blue-50, text-blue-700, text-xs).

       **Before/After comparison section:**
       - "Before" and "After" headings with side-by-side (or stacked on mobile) JSON display.
       - Parse `beforeSnapshot` and `afterSnapshot` JSON strings.
       - Render each as formatted JSON in a `<pre>` block with `font-mono text-xs` styling.
       - Use a simple diff highlighting approach: for each top-level key, compare JSON.stringify values between before and after. If different, highlight that key's line with a yellow background in the "before" panel and a green background in the "after" panel. This provides visual diff without importing a diff library (per zero-dependency project pattern).
       - If either snapshot is null (pre-Phase-8 baseline), show "Snapshot not available" placeholder.

       **Remediation link section:**
       - "Affected Controls" heading.
       - To link drift to remediation, determine which audit controls are now failing because of this drift. The approach (per research section 8):
         a. Import `getAllControls` from `@omzig/audit` (or the control registry).
         b. Parse the `afterSnapshot` as the relevant AuditFacts area.
         c. For each control that operates on this area, evaluate it against the after facts.
         d. Cross-reference with the `beforeSnapshot` to find controls that were passing before but are now failing.
         e. For each newly-failing control that has a remediation entry, render a `<RemediateButton>` with a synthetic `AuditFinding` object: `{ id: 'drift-'+alert.id+'-'+controlId, controlId, rating: 'fail', severity, ... }`.
       - **IMPORTANT:** If this client-side evaluation is too complex (many controls, large context), simplify to: display the diffSummary and a link "Run a new audit to see affected controls" with a button that navigates to the tenant audit page. Then show a "Remediate" link only if a `remediationJobId` is set on the alert (meaning the user already created a remediation from this drift). This simpler approach avoids importing the full evaluator set into the frontend bundle.
       - Claude's discretion (per CONTEXT): choose whichever approach fits the context budget. The simpler "link to re-audit" approach is acceptable if evaluator import is too heavy.

       **Actions section:**
       - "Dismiss" button (if not already dismissed). Calls `onDismiss(alertId)`.
       - "Close" button. Calls `onClose()`.

    2. Update `apps/web/src/components/tenants/ActionQueue.tsx`:
       - Add `'drift_detected'` to the `SEVERITY_BORDER` map:
         ```typescript
         drift_detected: 'border-l-purple-500',
         ```
       - Add a `TypeIcon` entry for `drift_detected`: use Material Symbols `sync_problem` icon (or lucide `Activity` if lucide is the icon library used in this component -- check existing icons).
       - Add rendering for `drift_detected` items:
         - Title: `"Drift Detected"` or `"${item.count} drift alert(s)"` if count is available.
         - Description: `item.tenantName` + " - Configuration drift detected".
         - Link: clicking navigates to `/drift?tenant=${item.tenantId}` (or `/drift` if no tenant filter is supported in the URL).
       - The action queue API (from Phase 5) already computes items server-side from control-plane data. After Plan 08-01 adds the `drift_detected` type to ActionQueueItem, the action-queue route will need to query drift_alerts counts from tenant DBs. **If the action-queue route does not yet include drift counting (it may not since Plan 08-01 focused on the drift-specific routes), add a note in the SUMMARY that the action-queue route needs a follow-up to include drift_detected items.** For the frontend, handle the type regardless -- if the API returns drift_detected items, render them; if not, no items appear and the component still works.

    3. Update `apps/web/src/app/tenants/page.tsx` (dashboard):
       - No changes needed if ActionQueue already renders all item types from the API. The ActionQueue component update in step 2 handles the new type.
       - If there is a "drift summary card" desired on the dashboard, add a small card above or alongside the ActionQueue showing "X drift alerts across Y tenants" with a link to /drift. Use the same data from the action queue API (filter items where type === 'drift_detected' and sum counts). This is a lightweight addition.

    4. Create test files:
       - `apps/web/src/__tests__/DriftAlertDetail.test.tsx`:
         - Mock fetch to return a full drift alert with beforeSnapshot and afterSnapshot JSON.
         - Assert renders area name, severity badge, activity type, actor UPN.
         - Assert renders diffSummary text.
         - Assert renders before/after JSON in pre blocks.
         - Assert renders Dismiss button for active alert.
         - Assert Dismiss button hidden for dismissed alert.
         - Assert renders latency notice text.

       - `apps/web/src/__tests__/useDriftAlerts.test.ts`:
         - Mock fetch and SignalR connection.
         - Test initial fetch returns alerts from API.
         - Test SignalR driftAlert handler prepends new alert to state when tenantId matches.
         - Test SignalR message for different tenantId is ignored.
         - Test dismissAlert sends POST and updates alert in local state.
         - Test severity filter changes trigger re-fetch.

    5. **Final integration check**: After both tasks, verify the full drift flow:
       - Sidebar shows "Drift" link with badge.
       - /drift page shows all drift alerts with filters.
       - Clicking an alert opens the detail drawer/panel.
       - Detail shows before/after diff with remediation link.
       - Action queue on /tenants shows drift items.
       - SignalR pushes new alerts in real-time.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/__tests__/DriftAlertDetail.test.tsx src/__tests__/useDriftAlerts.test.ts -x</automated>
  </verify>
  <done>
    DriftAlertDetail shows full detail with before/after JSON comparison, human-readable summary, actor info, severity badge, latency notice, and remediation link. ActionQueue renders drift_detected items with purple border and sync_problem icon. useDriftAlerts handles SignalR real-time push (prepend new alerts, ignore other tenants) and dismiss flow. Dashboard shows drift items in the action queue. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Browser -> API | All drift API calls go through auth middleware; frontend uses Bearer tokens from useAuth() |
| SignalR Hub -> Client | SignalR messages are scoped to userId via JWT nameid claim; no cross-user leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | DriftAlertDetail beforeSnapshot/afterSnapshot | mitigate | Snapshots are only accessible via authenticated tenant-scoped API routes. The frontend renders them but never stores them in localStorage or exposes to other origins. |
| T-08-08 | Spoofing | SignalR driftAlert messages | mitigate | SignalR connection is authenticated via user-scoped JWT from the negotiate endpoint. Messages are only pushed to users in tenantUserAccess for the affected tenant. Frontend validates tenantId match before updating state. |
| T-08-09 | Tampering | DriftConfigPanel interval save | mitigate | PATCH /drift-config is Admin-only (enforced server-side). Frontend disables Save for non-Admin users as defense-in-depth. Interval validated 15-60 range server-side. |
| T-08-10 | Denial of Service | useDriftAlerts cross-tenant fetch | accept | Cross-tenant mode fetches alerts for each accessible tenant in parallel. For MSPs with many tenants (50+), this could be slow. Acceptable for v1 -- v2 could add a server-side cross-tenant drift endpoint. |
</threat_model>

<verification>
1. `cd apps/web && pnpm vitest run src/__tests__/DriftAlertList.test.tsx src/__tests__/DriftAlertDetail.test.tsx src/__tests__/useDriftAlerts.test.ts -x` -- all frontend tests pass
2. `cd apps/web && pnpm test` -- full web test suite passes (no regressions)
3. `cd apps/web && pnpm tsc --noEmit` -- frontend compiles with all new drift components, hooks, and page
</verification>

<success_criteria>
- /drift page renders a filterable table of drift alerts across all accessible tenants
- DriftAlertList shows timestamp, severity badge, area, activity, actor, status for each alert
- DriftAlertDetail shows before/after JSON diff with highlighted changes, summary, actor, remediation link
- DriftSeverityBadge renders Critical (red), High (orange), Medium (yellow), Low (blue)
- DriftConfigPanel lets Admin users set polling interval (15/30/45/60 min)
- Sidebar has "Drift" nav item with sync_problem icon and alert count badge (red circle)
- ActionQueue renders drift_detected items with purple border and navigation to /drift
- SignalR driftAlert subscription updates the alert list in real-time without page refresh
- useDriftAlerts hook manages fetch, filters, SignalR subscription, and dismiss flow
- All frontend tests pass, no regressions in existing components
</success_criteria>

<output>
After completion, create `.planning/phases/08-drift-detection/08-02-SUMMARY.md`
</output>
