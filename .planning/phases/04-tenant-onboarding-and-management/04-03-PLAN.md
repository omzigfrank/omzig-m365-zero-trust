---
phase: 04-tenant-onboarding-and-management
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - apps/web/src/app/tenants/page.tsx
  - apps/web/src/app/tenants/[id]/page.tsx
  - apps/web/src/components/tenants/TenantCard.tsx
  - apps/web/src/components/tenants/TenantGrid.tsx
  - apps/web/src/components/tenants/TenantTable.tsx
  - apps/web/src/components/tenants/HealthDot.tsx
  - apps/web/src/components/tenants/RemoveTenantModal.tsx
  - apps/web/src/hooks/useTenants.ts
  - apps/web/src/lib/tenant-api.ts
  - apps/web/src/lib/types.ts
  - apps/web/src/__tests__/TenantDashboard.test.tsx
  - apps/web/src/__tests__/TenantDetail.test.tsx
autonomous: true
requirements:
  - TENANT-03
  - TENANT-04
  - TENANT-07

must_haves:
  truths:
    - "MSP can see all connected tenants in a card grid at /tenants"
    - "MSP can toggle between card grid and data table view"
    - "Each tenant card shows overall score, per-framework mini scores, last audit time, critical findings count, and colored health dot"
    - "MSP can click a tenant card to navigate to /tenants/:id which shows the full compliance dashboard"
    - "MSP can click Remove on a tenant and confirm deletion by typing the tenant name"
    - "Health dot color matches threshold spec: green >=70, yellow 40-69, red <40, gray no audit, orange needs reauth"
    - "Add Tenant card in the grid navigates to /tenants/new"
  artifacts:
    - path: "apps/web/src/app/tenants/page.tsx"
      provides: "Multi-tenant dashboard with card grid and table toggle"
      min_lines: 40
    - path: "apps/web/src/app/tenants/[id]/page.tsx"
      provides: "Tenant detail page wrapping Phase 3 compliance dashboard"
      min_lines: 30
    - path: "apps/web/src/components/tenants/TenantCard.tsx"
      provides: "Individual tenant card with scores, health dot, and actions"
      min_lines: 40
    - path: "apps/web/src/hooks/useTenants.ts"
      provides: "Hook to fetch and manage tenant list from API"
      exports: ["useTenants"]
    - path: "apps/web/src/lib/tenant-api.ts"
      provides: "API client functions for tenant endpoints"
      exports: ["fetchTenants", "deleteTenant", "fetchTenantDetail"]
  key_links:
    - from: "apps/web/src/hooks/useTenants.ts"
      to: "/api/tenants"
      via: "fetch in useEffect"
      pattern: "fetch.*api/tenants"
    - from: "apps/web/src/app/tenants/[id]/page.tsx"
      to: "apps/web/src/app/audit/page.tsx"
      via: "Reuses audit dashboard components with tenant context"
      pattern: "X-Tenant-Id"
    - from: "apps/web/src/components/tenants/TenantCard.tsx"
      to: "apps/web/src/components/tenants/HealthDot.tsx"
      via: "Component composition"
      pattern: "HealthDot"
---

<objective>
Build the multi-tenant dashboard at /tenants with hybrid card-grid/table view, tenant detail page at /tenants/:id wrapping the Phase 3 compliance dashboard, and tenant removal modal.

Purpose: This is the primary MSP interface for managing multiple client tenants. Without this, MSPs have no way to see or navigate between tenants.

Output: /tenants page with card grid + table toggle, /tenants/:id detail page reusing audit components, tenant API client, removal modal with name-typing confirmation.
</objective>

<execution_context>
@C:/Users/FrankDiaz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FrankDiaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tenant-onboarding-and-management/04-CONTEXT.md
@.planning/phases/04-tenant-onboarding-and-management/04-RESEARCH.md
@.planning/phases/04-tenant-onboarding-and-management/04-01-SUMMARY.md
@.planning/phases/04-tenant-onboarding-and-management/04-02-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 02 that this plan consumes -->

GET /api/tenants response:
```typescript
{
  data: TenantSummary[],
  meta: { correlationId: string, timestamp: string }
}
```

DELETE /api/tenants/:id response:
```typescript
{
  data: { id: string, status: 'suspended' },
  meta: { correlationId: string, timestamp: string }
}
```

From packages/shared/src/types/tenant.ts (created in Plan 01):
```typescript
export type TenantStatus = 'pending' | 'active' | 'needs_reauth' | 'suspended' | 'purged';
export type ConnectionMethod = 'oauth' | 'gdap';
export type TenantHealth = 'green' | 'yellow' | 'red' | 'gray' | 'orange';
export interface TenantSummary {
  id: string;
  displayName: string;
  m365TenantId: string;
  status: TenantStatus;
  connectionMethod: ConnectionMethod | null;
  primaryDomain: string | null;
  health: TenantHealth;
  overallScore: number | null;
  frameworkScores: {
    AAD?: number;
    ZTA?: number;
    '80053'?: number;
    CSF?: number;
  } | null;
  lastAuditAt: string | null;
  criticalFindingsCount: number;
  createdAt: string;
}
export function calculateHealth(overallScore: number | null, status: TenantStatus): TenantHealth;
```

From existing frontend patterns:
```typescript
// apps/web/src/hooks/useAuth.ts
export function useAuth(): { user: UserProfile | null, loading: boolean, ... };

// apps/web/src/lib/api.ts (existing authenticated fetch wrapper)
export async function apiFetch(path: string, options?: RequestInit): Promise<Response>;
// Sets Authorization: Bearer {token} and X-Tenant-Id header

// apps/web/src/app/audit/page.tsx -- existing compliance dashboard
// Contains: ScoreCards, ZtaRadarChart, FindingsList, FrameworkFilter
// Reuse by wrapping in /tenants/:id page with tenant context set
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build multi-tenant dashboard page with card grid, table toggle, and API client</name>
  <files>
    apps/web/src/app/tenants/page.tsx,
    apps/web/src/components/tenants/TenantCard.tsx,
    apps/web/src/components/tenants/TenantGrid.tsx,
    apps/web/src/components/tenants/TenantTable.tsx,
    apps/web/src/components/tenants/HealthDot.tsx,
    apps/web/src/components/tenants/RemoveTenantModal.tsx,
    apps/web/src/hooks/useTenants.ts,
    apps/web/src/lib/tenant-api.ts,
    apps/web/src/lib/types.ts,
    apps/web/src/__tests__/TenantDashboard.test.tsx
  </files>
  <behavior>
    - TenantDashboard renders loading state, then card grid after fetch completes
    - Each TenantCard shows displayName, health dot, overallScore (or '--' if null), framework mini scores, relative lastAuditAt
    - HealthDot renders correct color class for each health status
    - Toggle button switches between 'grid' and 'table' view
    - TenantTable renders columns: Name, Domain, Status, Score, Last Audit, Health
    - "Add Tenant" card in grid navigates to /tenants/new
    - RemoveTenantModal requires typing the exact tenant displayName to enable confirm button
    - useTenants hook returns { tenants, loading, error, removeTenant }
    - Clicking a tenant card navigates to /tenants/{id}
  </behavior>
  <action>
    **tenant-api.ts** (API client):
    - Import apiFetch from existing lib/api.ts
    - `fetchTenants(): Promise<TenantSummary[]>` -- GET /api/tenants, return data array
    - `deleteTenant(id: string): Promise<void>` -- DELETE /api/tenants/:id
    - `fetchTenantDetail(id: string): Promise<TenantSummary>` -- GET /api/tenants/:id, return data

    **types.ts** (extend existing):
    - Add `TenantSummaryFE` re-export from @omzig/shared or define locally if import doesn't work across packages. Use the same TenantSummary shape. Add to existing types file.

    **useTenants.ts** hook:
    - State: tenants (TenantSummary[]), loading (boolean), error (string | null)
    - useEffect to fetch on mount via fetchTenants()
    - `removeTenant(id: string)`: call deleteTenant, then filter tenant from local state (optimistic)
    - Return { tenants, loading, error, removeTenant, refetch }

    **HealthDot.tsx**:
    - Accept `health: TenantHealth` prop
    - Render a 12px circle with background color:
      - green: bg-emerald-500
      - yellow: bg-amber-500
      - red: bg-red-500
      - gray: bg-gray-400
      - orange: bg-orange-500
    - Add aria-label for accessibility: "Health: {health}"

    **TenantCard.tsx**:
    - Accept `tenant: TenantSummary, onRemove: (id: string) => void` props
    - Display: displayName, HealthDot, overall score (large number or '--'), 4 mini framework score bars (AAD, ZTA, 800-53, CSF as small text labels with scores or '--'), relative time for lastAuditAt (use simple formatter: minutes/hours/days ago), criticalFindingsCount if > 0 (with badge), status badge for non-active states
    - Click handler: router.push(`/tenants/${tenant.id}`)
    - Kebab menu or Remove button that opens RemoveTenantModal
    - Use Tailwind: rounded-lg border shadow-sm hover:shadow-md transition

    **TenantGrid.tsx**:
    - Accept `tenants: TenantSummary[], onRemove: (id: string) => void` props
    - Render CSS grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4
    - First position: "Add Tenant" card (dashed border, plus icon, links to /tenants/new)
    - Map tenants to TenantCard components

    **TenantTable.tsx**:
    - Accept `tenants: TenantSummary[], onRemove: (id: string) => void` props
    - Render HTML table with columns: Name, Domain, Status, Overall Score, Last Audit, Health, Actions
    - Sortable by clicking column headers (client-side sort, default by displayName)
    - Name column is a link to /tenants/:id
    - Actions column has Remove button
    - Use Tailwind table styling consistent with existing app

    **RemoveTenantModal.tsx**:
    - Accept `tenant: TenantSummary | null, isOpen: boolean, onConfirm: () => void, onCancel: () => void` props
    - Display warning message: "This will remove {tenant.displayName} and schedule all data for deletion after 30 days."
    - Text input: "Type the tenant name to confirm"
    - Confirm button disabled until input === tenant.displayName (case-sensitive)
    - Confirm button is red/destructive styled
    - Cancel button closes modal

    **page.tsx** (dashboard):
    - Wrap in AuthGuard (existing pattern)
    - Use useTenants() hook
    - Toggle state: 'grid' | 'table' (default: 'grid')
    - Header: "Tenants" title, tenant count badge, view toggle buttons (grid/table icons)
    - Body: conditional render TenantGrid or TenantTable based on toggle
    - Loading state: skeleton cards
    - Error state: error message with retry button
    - Empty state: "No tenants connected. Add your first tenant to get started." with Add Tenant button
    - RemoveTenantModal (single instance, controlled by selectedTenant state)

    **Tests:**
    - Mock useTenants to return fixture TenantSummary array
    - Test card grid renders correct number of cards + 1 "Add Tenant" card
    - Test health dot colors match fixture data
    - Test toggle switches from grid to table
    - Test RemoveTenantModal confirm button is disabled until name matches
    - Test clicking card navigates to /tenants/:id (mock router.push)
  </action>
  <verify>
    <automated>cd C:/Users/FrankDiaz/omzig-m365-zero-trust && pnpm --filter @omzig/web vitest run src/__tests__/TenantDashboard.test.tsx -x</automated>
  </verify>
  <done>
    - /tenants page renders card grid by default with all connected tenants
    - Toggle switches between card grid and data table views
    - Each card shows health dot, score, framework mini scores, last audit time
    - "Add Tenant" card in grid links to /tenants/new
    - Remove modal requires typing tenant name to confirm
    - useTenants hook fetches from API and supports optimistic removal
    - Tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build tenant detail page wrapping Phase 3 compliance dashboard</name>
  <files>
    apps/web/src/app/tenants/[id]/page.tsx,
    apps/web/src/__tests__/TenantDetail.test.tsx
  </files>
  <behavior>
    - /tenants/:id page reads tenant ID from URL params
    - Sets X-Tenant-Id context for all API calls within the page
    - Renders breadcrumb: Tenants > {tenant.displayName}
    - Displays tenant info header: name, domain, status badge, health dot, connection method badge
    - Renders tabs: Overview, Findings, Audit History, Settings
    - Overview tab reuses ScoreCards and ZtaRadarChart from existing audit page
    - Findings tab reuses FindingsList with FrameworkFilter from existing audit page
    - Clicking "Tenants" breadcrumb navigates back to /tenants
    - Shows loading state while fetching tenant detail
    - Shows 404 message if tenant not found
  </behavior>
  <action>
    **page.tsx** (`apps/web/src/app/tenants/[id]/page.tsx`):
    - Use Next.js dynamic route params to get tenant ID
    - Fetch tenant detail via fetchTenantDetail(id) from tenant-api.ts
    - Render a TenantDetailLayout:

    1. **Breadcrumb bar**: Tenants (link to /tenants) > {tenant.displayName}

    2. **Tenant header section**:
       - Display name (large), primary domain (small text), HealthDot + status badge
       - Connection method badge ('OAuth' or 'GDAP' pill)
       - Last audit timestamp
       - "Run Audit" button (reuses existing audit trigger pattern from useAudit hook)
       - "Settings" gear icon (placeholder for future)

    3. **Tab navigation** (Overview | Findings | Audit History | Settings):
       - Use client-side tab state (no URL routing for tabs)
       - **Overview tab**: Render existing ScoreCards component and ZtaRadarChart component from `apps/web/src/app/audit/page.tsx` area. Import these components. Pass the latest audit data. Use the existing useAudit hook with the tenantId to fetch audit data.
       - **Findings tab**: Render existing FrameworkFilter and FindingsList components. Pass findings from the latest audit.
       - **Audit History tab**: List of past audit runs (id, startedAt, status, total/pass/fail counts). Use GET /api/tenants/:tenantId/audits (existing audit list endpoint with X-Tenant-Id header).
       - **Settings tab**: Placeholder "Tenant settings coming in future phases" with tenant metadata display (m365TenantId, connectionMethod, createdAt).

    4. **Key pattern**: Set the tenant ID in a React context or use the existing authenticated API client with X-Tenant-Id header. The existing `apiFetch` function already supports setting X-Tenant-Id. The useAudit hook already accepts a tenantId parameter. Wire the URL param id through to useAudit(tenantId).

    5. **Loading/error states**: Show skeleton while fetching, show 404-style message if tenant not found, show error state if API fails.

    The goal is REUSE, not rebuild. The Phase 3 compliance dashboard components (ScoreCards, ZtaRadarChart, FindingsList, FrameworkFilter) already exist and work. This page wraps them in a tenant-scoped context with navigation chrome.

    **Tests:**
    - Mock fetchTenantDetail to return a fixture TenantSummary
    - Mock useAudit to return fixture audit data
    - Test breadcrumb renders with tenant name
    - Test tab navigation switches content
    - Test tenant header shows correct health dot and status
    - Test that tenant ID from URL params is passed to useAudit
  </action>
  <verify>
    <automated>cd C:/Users/FrankDiaz/omzig-m365-zero-trust && pnpm --filter @omzig/web vitest run src/__tests__/TenantDetail.test.tsx -x</automated>
  </verify>
  <done>
    - /tenants/:id renders tenant detail with breadcrumb navigation
    - Overview tab shows ScoreCards and ZtaRadarChart from Phase 3
    - Findings tab shows FrameworkFilter and FindingsList
    - Audit History tab lists past audit runs
    - Tenant header shows health dot, status badge, connection method
    - Tenant ID from URL params wired to useAudit for data fetching
    - Tests pass
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @omzig/web vitest run src/__tests__/TenantDashboard.test.tsx src/__tests__/TenantDetail.test.tsx -x` -- all frontend tests pass
- `pnpm --filter @omzig/web exec tsc --noEmit` -- frontend compiles
- `pnpm -r test` -- full suite green
</verification>

<success_criteria>
- /tenants page shows all connected tenants in card grid with health dots and scores
- Toggle switches between card grid and data table views
- Each card navigable to /tenants/:id
- Tenant detail page wraps Phase 3 compliance dashboard with tenant context
- Breadcrumb navigation between /tenants and /tenants/:id
- Remove modal with name-typing confirmation
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-tenant-onboarding-and-management/04-03-SUMMARY.md`
</output>
