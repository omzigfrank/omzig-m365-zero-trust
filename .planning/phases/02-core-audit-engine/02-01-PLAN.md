---
phase: 02-core-audit-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/audit/package.json
  - packages/audit/tsconfig.json
  - packages/audit/vitest.config.ts
  - packages/audit/src/index.ts
  - packages/audit/src/types.ts
  - packages/audit/src/registry/entra-id-controls.ts
  - packages/audit/src/registry/control-registry.ts
  - packages/audit/src/collectors/graph-client.ts
  - packages/audit/src/collectors/batch-helper.ts
  - packages/audit/src/collectors/fact-collector.ts
  - packages/audit/src/collectors/areas/conditional-access.ts
  - packages/audit/src/collectors/areas/authentication-methods.ts
  - packages/audit/src/collectors/areas/authorization-policy.ts
  - packages/audit/src/collectors/areas/directory-roles.ts
  - packages/audit/src/collectors/areas/devices.ts
  - packages/audit/src/collectors/areas/licenses.ts
  - packages/audit/src/collectors/areas/domains.ts
  - packages/audit/src/collectors/areas/organization.ts
  - packages/audit/src/collectors/areas/security-defaults.ts
  - packages/audit/src/collectors/areas/pim-roles.ts
  - packages/audit/src/collectors/areas/app-registrations.ts
  - packages/audit/src/collectors/areas/sensitivity-labels.ts
  - packages/audit/src/__tests__/fact-collector.test.ts
  - packages/audit/src/__tests__/control-registry.test.ts
  - packages/audit/src/__tests__/fixtures/graph-responses.ts
autonomous: true
requirements: [AUDIT-02, AUDIT-03, AUDIT-04, AUDIT-06, FRAME-01, AUTH-06]

must_haves:
  truths:
    - "AuditFacts type covers all 15 Graph API data areas needed by 29 Entra ID evaluators"
    - "Control registry defines all 29 CISA SCuBA Entra ID controls with ID, severity, requirement level, and required Graph permissions"
    - "Fact collector produces a complete AuditFacts snapshot from a Graph client using $batch optimization"
    - "Each control definition includes NIST 800-53 cross-reference and required permission mapping"
    - "Graph batch helper correctly handles per-request failures (status >= 400 sets available=false, not crash)"
  artifacts:
    - path: "packages/audit/src/types.ts"
      provides: "AuditFacts, EvaluatorResult, EvaluatorFn, ControlDefinition types"
      exports: ["AuditFacts", "EvaluatorResult", "EvaluatorFn", "ControlDefinition"]
    - path: "packages/audit/src/registry/entra-id-controls.ts"
      provides: "29 CISA SCuBA Entra ID control definitions"
      contains: "ENTRA_ID_CONTROLS"
    - path: "packages/audit/src/collectors/fact-collector.ts"
      provides: "collectFacts function producing complete AuditFacts"
      exports: ["collectFacts"]
    - path: "packages/audit/src/collectors/graph-client.ts"
      provides: "Graph SDK client factory with delegated token auth"
      exports: ["createGraphClient"]
  key_links:
    - from: "packages/audit/src/registry/entra-id-controls.ts"
      to: "packages/audit/src/types.ts"
      via: "imports ControlDefinition type"
      pattern: "import.*ControlDefinition.*from.*types"
    - from: "packages/audit/src/collectors/fact-collector.ts"
      to: "packages/audit/src/collectors/batch-helper.ts"
      via: "uses executeBatch for Graph API calls"
      pattern: "import.*executeBatch.*from.*batch-helper"
    - from: "packages/audit/src/collectors/fact-collector.ts"
      to: "packages/audit/src/types.ts"
      via: "returns AuditFacts"
      pattern: "Promise<AuditFacts>"
---

<objective>
Create the `packages/audit` package with the core types, CISA SCuBA control registry, and Graph API fact collector that form the foundation of the audit engine.

Purpose: Establishes all type contracts and the "collect" half of the collect-then-evaluate pipeline. Pure TypeScript with no database or API coupling -- evaluators (Plan 03) and API routes (Plan 02) build on these contracts.

Output: `packages/audit` package with AuditFacts type, 29 control definitions, Graph batch helper, and fact collector function. Verified by unit tests.
</objective>

<execution_context>
@C:/Users/FrankDiaz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FrankDiaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-audit-engine/02-CONTEXT.md
@.planning/phases/02-core-audit-engine/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase that this plan depends on. -->

From packages/db/src/connection.ts:
```typescript
export type TenantDb = NodeMsSqlDatabase<typeof tenantSchema>;
export async function getTenantDb(databaseName: string): Promise<{ db: TenantDb; pool: mssql.ConnectionPool }>;
export async function closeTenantDb(pool: mssql.ConnectionPool): Promise<void>;
```

From packages/db/src/tenant/schema.ts:
```typescript
export const auditRuns = mssqlTable('audit_runs', {
  id: varchar('id', { length: 36 }).primaryKey().notNull(),
  tenantId: varchar('tenant_id', { length: 36 }).notNull(),
  triggeredBy: varchar('triggered_by', { length: 36 }).notNull(),
  status: varchar('status', { length: 20 }).notNull().default('pending'),
  startedAt: datetime2('started_at'),
  completedAt: datetime2('completed_at'),
  totalChecks: int('total_checks').notNull().default(0),
  passedChecks: int('passed_checks').notNull().default(0),
  failedChecks: int('failed_checks').notNull().default(0),
  errorChecks: int('error_checks').notNull().default(0),
  summary: nvarchar('summary', { length: 4000 }),
  createdAt: datetime2('created_at').notNull().default(sql`GETDATE()`),
});
```

From apps/api/src/services/keyvault.ts:
```typescript
export async function getTenantToken(tenantId: string): Promise<string>;
export async function storeTenantToken(tenantId: string, token: string): Promise<void>;
export async function encryptValue(value: string): Promise<string>;
export async function decryptValue(encrypted: string): Promise<string>;
```

Port sources (PowerShell evaluator logic):
- scripts/audit/CisaEvaluatorRegistry.ps1 -- 16 evaluators with complete pass/fail logic
- scripts/audit/TenantFactCollector.ps1 -- 15 Graph API data areas collected into facts object
- scripts/audit/CisaCatalogFetcher.ps1 -- control metadata (IDs, descriptions, severity, NIST mappings)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create audit package with types, control registry, and graph client</name>
  <files>
    packages/audit/package.json
    packages/audit/tsconfig.json
    packages/audit/vitest.config.ts
    packages/audit/src/index.ts
    packages/audit/src/types.ts
    packages/audit/src/registry/entra-id-controls.ts
    packages/audit/src/registry/control-registry.ts
    packages/audit/src/collectors/graph-client.ts
    packages/audit/src/collectors/batch-helper.ts
    packages/audit/src/__tests__/control-registry.test.ts
    packages/audit/src/__tests__/fixtures/graph-responses.ts
  </files>
  <behavior>
    - Test: ENTRA_ID_CONTROLS has exactly 29 entries
    - Test: Every control has id matching MS.AAD.*.* pattern, product === 'AAD'
    - Test: Every control has severity in ['Critical', 'High', 'Medium', 'Low']
    - Test: Every control has requirementLevel in ['SHALL', 'SHALL NOT', 'SHOULD', 'SHOULD NOT', 'MAY']
    - Test: Every control has non-empty requiredPermissions array
    - Test: Every control has nist80053 cross-reference string
    - Test: Every control has an evaluator function (type EvaluatorFn)
    - Test: getControlById returns correct control or undefined
    - Test: getControlsByProduct('AAD') returns all 29
    - Test: Graph client factory creates Client with auth provider using provided access token
    - Test: Batch helper groups requests correctly and handles per-request errors (status >= 400 sets error flag)
  </behavior>
  <action>
    1. Create `packages/audit/package.json` with name `@omzig/audit`, dependencies: `@microsoft/microsoft-graph-client`, `@microsoft/microsoft-graph-types`, `@microsoft/microsoft-graph-types-beta`. DevDependencies: vitest, typescript, @omzig/tsconfig. Add build/test scripts matching existing package patterns.

    2. Create `packages/audit/tsconfig.json` extending @omzig/tsconfig with exclude for test files (same pattern as packages/db).

    3. Create `packages/audit/vitest.config.ts` matching the packages/db vitest config pattern.

    4. Create `packages/audit/src/types.ts` with these types (ported from research):
       - `AuditFacts`: 15 data areas (organization, conditionalAccess, namedLocations, mfa, authMethods, authorizationPolicy, adminConsentPolicy, passwordPolicy, adminRoles, roleAssignments, devices, licenses, securityDefaults, domains, appRegistrations, sensitivityLabels). Each area has `available: boolean`, area-specific data fields, and `error?: string`. Port structure from TenantFactCollector.ps1.
       - `EvaluatorResult`: rating (pass/fail/warn/na), message, action?, settingName?, currentValue?, expectedValue?, requiredPermission?
       - `EvaluatorFn`: `(facts: AuditFacts) => EvaluatorResult`
       - `ControlDefinition`: id, product, description, requirementLevel (SHALL/SHALL NOT/SHOULD/SHOULD NOT/MAY), severity (Critical/High/Medium/Low), nist80053, evaluator (EvaluatorFn), requiredPermissions (string[])
       - `AuditProgressMessage`: auditId, tenantId, completed, total, currentCheck, status (running/complete/error)

    5. Create `packages/audit/src/registry/entra-id-controls.ts` with ALL 29 CISA SCuBA Entra ID control definitions. Port control metadata from CisaCatalogFetcher.ps1 and CisaEvaluatorRegistry.ps1. Each control has:
       - id (e.g., 'MS.AAD.1.1v1'), product ('AAD'), description, requirementLevel, severity, nist80053
       - evaluator: placeholder function returning {rating: 'na', message: 'Evaluator not yet implemented'} (real evaluators wired in Plan 03)
       - requiredPermissions: array of Graph scopes needed for this check (from RESEARCH.md permission map)

       Controls: MS.AAD.1.1v1 (legacy auth, Critical, SHALL, AC-7), MS.AAD.2.1v1 (high-risk users, Critical, SHALL, AC-7), MS.AAD.2.3v1 (high-risk sign-ins, Critical, SHALL, AC-7), MS.AAD.3.1v1 (MFA all users, Critical, SHALL, IA-2), MS.AAD.3.2v1 (alt MFA method, High, SHOULD, IA-2), MS.AAD.3.3v1 (alt MFA enforced, Medium, SHOULD, IA-2), MS.AAD.3.4v1 (auth methods migration, Medium, SHALL, IA-2), MS.AAD.3.5v1 (no SMS/voice, High, SHALL, IA-2), MS.AAD.3.6v1 (admin phishing-resistant, Critical, SHALL, IA-2), MS.AAD.3.7v1 (managed devices, Medium, SHOULD, CM-6), MS.AAD.3.8v1 (managed devices MFA reg, Medium, SHOULD, CM-6), MS.AAD.4.1v1 (security logs SIEM, High, SHALL, AU-6), MS.AAD.5.1v1 (only admins register apps, High, SHALL, CM-7), MS.AAD.5.2v1 (only admins consent, High, SHALL, CM-7), MS.AAD.5.3v1 (admin consent workflow, Medium, SHOULD, CM-7), MS.AAD.5.4v1 (no group owner consent, Medium, SHALL NOT, CM-7), MS.AAD.6.1v1 (passwords no expire, Medium, SHALL, IA-5), MS.AAD.7.1v1 (2-8 global admins, Critical, SHALL, AC-2), MS.AAD.7.2v1 (PIM for privileged, High, SHALL, AC-2), MS.AAD.7.3v1 (GA activation approval, High, SHOULD, AC-2), MS.AAD.7.4v1 (monthly role audit, Medium, SHOULD, AC-2), MS.AAD.7.5v1 (no permanent active, High, SHOULD, AC-2), MS.AAD.7.6v1 (MFA privileged activation, High, SHOULD, AC-2), MS.AAD.7.7v1 (no assignments outside PIM, High, SHOULD, AC-2), MS.AAD.7.8v1 (alert on privileged activation, Medium, SHOULD, AC-2), MS.AAD.7.9v1 (GA activation needs approval, High, SHOULD, AC-2), MS.AAD.8.1v1 (restrict guest access, High, SHALL, AC-3), MS.AAD.8.2v1 (admin-only guest invites, High, SHOULD, AC-3), MS.AAD.8.3v1 (restrict guest user access, Medium, SHOULD, AC-3).

    6. Create `packages/audit/src/registry/control-registry.ts` with helper functions:
       - `getControlById(id: string): ControlDefinition | undefined`
       - `getControlsByProduct(product: string): ControlDefinition[]`
       - `getAllControls(): ControlDefinition[]`

    7. Create `packages/audit/src/collectors/graph-client.ts`: factory function `createGraphClient(accessToken: string): Client` using `@microsoft/microsoft-graph-client` Client.init with authProvider callback. Per user decision, use the official Graph SDK.

    8. Create `packages/audit/src/collectors/batch-helper.ts`: `executeBatch(client: Client, requests: BatchRequest[]) => Promise<Map<string, any>>` that:
       - Groups requests into batches of max 20 (Graph API limit)
       - Uses BatchRequestContent/BatchRequestStep for each batch
       - Checks EACH per-request response status (PITFALL 1: $batch outer is always 200)
       - Returns Map with response data or `{ error: true, status: number }` for failed requests
       - Type `BatchRequest = { id: string; url: string; method?: string }`

    9. Create `packages/audit/src/__tests__/fixtures/graph-responses.ts` with typed mock Graph API response objects:
       - Mock CA policies array (1 enabled blocking legacy auth, 1 requiring MFA)
       - Mock organization response, subscribedSkus, domains, auth methods policy
       - Mock directoryRoles with Global Administrator, mock authorization policy
       - Mock batch response wrapper

    10. Create `packages/audit/src/index.ts` exporting all public types and functions.

    11. Run `pnpm install` from monorepo root to link the new package.
  </action>
  <verify>
    <automated>cd packages/audit && pnpm test</automated>
  </verify>
  <done>
    - packages/audit exists as a workspace package with build passing
    - 29 control definitions in ENTRA_ID_CONTROLS with correct metadata
    - AuditFacts type covers all 15 Graph API data areas
    - Graph client factory and batch helper tested with mock responses
    - All control-registry tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Implement core area collectors (organization, CA, auth methods, auth policy, directory roles, security defaults)</name>
  <files>
    packages/audit/src/collectors/areas/organization.ts
    packages/audit/src/collectors/areas/conditional-access.ts
    packages/audit/src/collectors/areas/authentication-methods.ts
    packages/audit/src/collectors/areas/authorization-policy.ts
    packages/audit/src/collectors/areas/directory-roles.ts
    packages/audit/src/collectors/areas/security-defaults.ts
  </files>
  <behavior>
    - Test: organization area parses /organization collection response and extracts tenantId, displayName, primaryDomain
    - Test: conditional-access area parses policies collection response into typed ConditionalAccessPolicy[]
    - Test: authentication-methods area parses both batch result (authenticationMethodsPolicy) and standalone paginated result (MFA registration -- PITFALL 7)
    - Test: authorization-policy area correctly parses singleton response (not collection -- PITFALL 6)
    - Test: directory-roles area parses roles collection and extracts Global Administrator role ID
    - Test: security-defaults area parses identitySecurityDefaultsEnforcementPolicy singleton
    - Test: Each area sets available=true on success
    - Test: Each area sets available=false with error message on Graph call failure (403/404)
  </behavior>
  <action>
    Create 6 core area collector files in `packages/audit/src/collectors/areas/`. These are the primary areas needed by the most critical evaluators (legacy auth, risk policies, MFA, app registration, guest access). Port logic from TenantFactCollector.ps1 (lines 34-200+). Each area collector is a function that takes a Map of batch results (or standalone Graph call results) and returns the typed facts area.

    Each area parser:
    - Checks if its batch request succeeded (result !== error)
    - Sets `available = true` and populates typed fields on success
    - Sets `available = false` with error message on failure
    - Handles both collection responses (has `value` array) and singleton responses (direct object) -- PITFALL 6

    Area files and their Graph endpoints:
    - `organization.ts`: `/organization?$select=id,displayName,verifiedDomains`
    - `conditional-access.ts`: `/identity/conditionalAccess/policies?$select=id,displayName,state,conditions,grantControls,sessionControls&$top=100`
    - `authentication-methods.ts`: `/policies/authenticationMethodsPolicy` (batch) + `/reports/authenticationMethods/userRegistrationDetails` (standalone paginated -- PITFALL 7)
    - `authorization-policy.ts`: `/policies/authorizationPolicy` (singleton -- PITFALL 6)
    - `directory-roles.ts`: `/directoryRoles?$select=id,displayName&$top=100` (batch 1), then `/directoryRoles/{globalAdminId}/members` (batch 2, needs role ID from batch 1)
    - `security-defaults.ts`: `/policies/identitySecurityDefaultsEnforcementPolicy`
  </action>
  <verify>
    <automated>cd packages/audit && pnpm test</automated>
  </verify>
  <done>
    - 6 core area collectors parse Graph responses into typed AuditFacts areas
    - Singleton vs collection response handling correct (PITFALL 6)
    - MFA registration uses standalone paginated call (PITFALL 7)
    - All areas degrade gracefully on permission failures
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Implement extended area collectors (devices, licenses, domains, PIM, apps, labels) and fact collector</name>
  <files>
    packages/audit/src/collectors/areas/devices.ts
    packages/audit/src/collectors/areas/licenses.ts
    packages/audit/src/collectors/areas/domains.ts
    packages/audit/src/collectors/areas/pim-roles.ts
    packages/audit/src/collectors/areas/app-registrations.ts
    packages/audit/src/collectors/areas/sensitivity-labels.ts
    packages/audit/src/collectors/fact-collector.ts
    packages/audit/src/__tests__/fact-collector.test.ts
  </files>
  <behavior>
    - Test: devices area parses managedDevices collection and counts compliant vs total
    - Test: licenses area parses subscribedSkus and detects P2, Defender for O365 SKUs
    - Test: domains area parses domains collection and extracts passwordValidityPeriodInDays
    - Test: pim-roles area handles 403 gracefully when P2 not licensed (PITFALL 2), sets available=false
    - Test: app-registrations area parses count from $count=true response
    - Test: sensitivity-labels area uses beta endpoint URL (not v1.0 -- PITFALL 3)
    - Test: collectFacts produces AuditFacts with all 15 areas populated from mock Graph responses
    - Test: Progress callback is invoked with collection phase messages
    - Test: Batch 1 (independent endpoints) and Batch 2 (dependent endpoints) execute in order
  </behavior>
  <action>
    1. Create 6 extended area collector files in `packages/audit/src/collectors/areas/`:
       - `devices.ts`: `/deviceManagement/managedDevices?$select=id,complianceState&$top=999`
       - `licenses.ts`: `/subscribedSkus` -- parse SKU names using the map from TenantFactCollector.ps1
       - `domains.ts`: `/domains?$select=id,isDefault,isVerified,passwordValidityPeriodInDays,passwordNotificationWindowInDays`
       - `pim-roles.ts`: beta `/roleManagement/directory/roleEligibilityScheduleInstances` and `/roleManagement/directory/roleAssignmentScheduleInstances` -- must handle 403 for non-P2 tenants (PITFALL 2)
       - `app-registrations.ts`: `/applications?$top=1&$count=true&$select=id` (just count)
       - `sensitivity-labels.ts`: beta `/security/informationProtection/sensitivityLabels?$top=100&$select=id,name` -- standalone call, cannot batch with v1.0 (PITFALL 3)

    2. Create `packages/audit/src/collectors/fact-collector.ts` with `collectFacts(client: Client, progressFn: (msg: string) => void): Promise<AuditFacts>`:
       - Creates empty AuditFacts using `createEmptyFacts()` helper
       - Executes Batch 1 (12 independent Graph endpoints) via `executeBatch`
       - Parses batch 1 results using area parsers (from Task 2a and 2b area files)
       - Extracts Global Administrator role ID from batch 1 results
       - Executes Batch 2 (dependent calls: GA members, MFA registration details, PIM endpoints)
       - MFA registration: standalone paginated call outside batch (PITFALL 7), follow @odata.nextLink
       - Sensitivity labels: standalone call to beta endpoint (PITFALL 3)
       - PIM endpoints: standalone calls to beta, wrapped in try/catch for P2 licensing (PITFALL 2)
       - Invokes progressFn at each phase ("Collecting tenant configuration...", "Collecting user and device data...")
       - Returns complete AuditFacts snapshot

    3. Create comprehensive test file `packages/audit/src/__tests__/fact-collector.test.ts`:
       - Mock the Graph client (vi.mock @microsoft/microsoft-graph-client)
       - Provide fixture Graph responses for each endpoint
       - Test happy path: all 15 areas populated correctly
       - Test permission failures: individual areas degrade gracefully
       - Test PIM P2 requirement: 403 handled, facts.roleAssignments.available=false
       - Test singleton vs collection response parsing
       - Test progress callback invocation

    4. Update `packages/audit/src/index.ts` to export collectFacts, createGraphClient, and area types.
  </action>
  <verify>
    <automated>cd packages/audit && pnpm test</automated>
  </verify>
  <done>
    - 6 extended area collectors parse Graph responses into typed AuditFacts areas
    - PIM handles 403 for non-P2 tenants (PITFALL 2)
    - Sensitivity labels use beta endpoint (PITFALL 3)
    - collectFacts produces complete AuditFacts from mock Graph responses with all 15 areas
    - Progress callback invoked during collection phases
    - All fact-collector tests pass alongside existing control-registry tests
  </done>
</task>

</tasks>

<verification>
- `cd packages/audit && pnpm build` compiles without errors
- `cd packages/audit && pnpm test` -- all tests pass
- `pnpm build` from monorepo root succeeds (new package integrates)
- AuditFacts type covers all 15 data areas from TenantFactCollector.ps1
- ENTRA_ID_CONTROLS has exactly 29 entries matching CisaCatalogFetcher.ps1 control IDs
</verification>

<success_criteria>
- packages/audit is a functional workspace package with types, registry, and fact collector
- 29 CISA SCuBA Entra ID controls defined with complete metadata (id, severity, requirement level, permissions, NIST cross-ref)
- Fact collector tested against mock Graph responses with graceful degradation for permission failures
- Graph batch helper correctly handles per-request errors within $batch responses
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-audit-engine/02-01-SUMMARY.md`
</output>
